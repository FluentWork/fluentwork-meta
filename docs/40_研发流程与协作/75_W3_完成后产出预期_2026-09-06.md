# FluentWork V2.0 W3 完成后产出预期 · 验收与交付总览

**版本**:V1.0　**日期**:2026-09-06　**性质**:W3 收口前的产出预期 + 验收标准 + 跨仓交付清单
**对应**:`73_` V2.0 实施蓝图 §五(W3-W7 节奏) / `55_` W3 backend 启动包 / `60_` iOS W3-W4 启动包 / `74_` V2.0 凭证与 P0 修复验证日志
**上游**:`53_` V2.0 启动前待办清单 §五之三 F-6(启动前门禁)
**下游**:W4 启动包、B16 AIOrchestrator 接入、B8 review worker 落地
**目标读者**:后端 maintainer / iOS maintainer / 商务 / infra / 测试组 / PM

---

## 〇、TL;DR · W3 末必须真实可用的能力

W3 完成后,**最关键的 4 项能力必须真实可工作**(不是 stub):

| 能力 | 路径 | 对应 Issue |
|---|---|---|
| 🎙️ **流式 TTS 播放** | voicegateway 推送 Opus → iOS 解码播放 | **B17 + I15** |
| 🧠 **B7 命中注入 LLM** | B19 命中信号 → RecentHits → LLM 上下文 | **B19** |
| 📌 **F3 收藏置顶 PATCH 拆分** | PATCH /corpus/:id {pin/favorite} | **B25** |
| 🔒 **闪测 + A4 一键删除** | GET /drill/round + DELETE /account/data | **B22** |

外加 2 项 iOS 端 V2.0 收口:
- **I20** Prompt 工程师(turn 超时兜底 + outcome 对齐)
- **I21** 状态机 7 状态扩展

---

## 一、W3 范围(7 个一级 Issue,共 36+ sub-ticket)

### 1.1 Backend(4 个 master + 24 个 sub-ticket)

| Issue | GitHub | Sub-tickets | 估时 | 阻塞 |
|---|---|---|---|---|
| **B17** TTS Provider + 流式集成 | #44 (+ #52-#56) | 5 | 1.5d | C-2 火山 TTS 凭证 |
| **B19** B7 命中信号 → LLM 上下文注入 | #46 (+ #57-#60) | 4 | 1.0d | 0009/0010 迁移 |
| **B25** F3 收藏置顶(PATCH 拆分) | #51 (+ #61-#63) | 3 | 1.0d | D-API-1 已拍板 |
| **B22** 闪测 + A4 隐私删除 | #48 (+ #64-#73) | 10 | 2.5d | C-1 Ark Mini 凭证 + 0011/0012 迁移 |
| **小计** | 4 + 22 | **22** | **6.0d** | — |

### 1.2 iOS(3 个 master + 13 个 sub-ticket)

| Issue | 仓库本地 | Sub-tickets | 估时 | 阻塞 |
|---|---|---|---|---|
| **I20** Prompt 工程师接入 | `01-skill-I20-prompt.md` | 4 | 0.5d | 无 |
| **I21** 状态机子状态扩展 | `02-skill-I21-state-machine.md` | 4 | 0.5d | 无(但依赖 I20 的 TurnOutcome) |
| **I15** 说的房间 TTS 播放集成 | `03-skill-I15-tts.md` | 5 | 1.0d | **B17 CLOSED** |
| **小计** | 3 | **13** | **2.0d** | — |

### 1.3 W3 总工时

```
backend  6.0 dev-day
iOS      2.0 dev-day
infra    2.0 dev-day(监控 + TOS bucket + 灾备 region 复制)
───────────────────
合计    10.0 dev-day
```

---

## 二、Backend 必须达成的产出

### 2.1 B17 TTS Provider(火山 TTS + 流式推送)

**仓库新增代码**(以 `voicegateway/` 为核心):

```
voicegateway/
├── provider/
│   ├── tts.go                       # Provider 接口(统一抽象)
│   ├── tts_volcengine.go            # 火山 TTS 实现(主)
│   └── tts_dev_echo.go              # dev 环境 echo 实现(测试用)
├── stream/
│   ├── tts_stream.go                # 流式推送管理
│   ├── opus_encoder.go              # PCM → Opus 帧编码
│   └── voice_id_router.go           # D-2 拍板音色表路由
└── wss/
    └── frame_ai_tts_audio.go        # ai.tts.audio 帧定义
```

**必须可工作的能力**:
- ✅ Provider 接口统一,支持 1 主 + N fallback
- ✅ 流式推送 Opus 帧(每帧 20ms,P90 首字 ≤ 400ms)
- ✅ 4 个 D-2 音色全部接通(zh_male_tech_01 / en_female_professional / en_male_narrator / en_female_clear)
- ✅ 客户端通过 `client.tts.start` 帧请求,backend 通过 `ai.tts.start/audio/end` 帧推送
- ✅ 火山 QPS 限流时自动 fallback(DevEcho 模式降级)

**性能指标**:
| 指标 | 目标 | 实测 |
|---|---|---|
| 首字延迟 P90 | ≤ 400ms | 待 W3 末验收 |
| 单 session TTS QPS | ≤ 5 | (单用户会话级,不在峰值测试) |
| 火山 QPS 配额使用率 | ≤ 70% | (留 30% 余量给闪测等) |

### 2.2 B19 B7 命中信号注入 LLM 上下文

**仓库新增代码**:

```
services/
├── hit_aggregator.go                # 命中聚合(0010 phrase_block_uses 同步 0009 total_uses)
├── recent_hits_cache.go             # Redis 缓存最近 8 个 hit
└── prompt_injector.go               # 注入 RecentHits 到 LLM 上下文

api/
├── internal/
│   ├── hits.go                      # POST /internal/v1/hits(写入 phrase_block_uses)
│   └── recent_hits.go               # GET /internal/v1/recent-hits
└── migrations/
    ├── 0009_alter_phrase_blocks_add_total_uses.sql    # 已就绪
    ├── 0010_create_phrase_block_uses.sql                # 已就绪
    └── 0010_alter_phrase_block_uses_add_index.sql      # W3 实施时补索引
```

**必须可工作的能力**:
- ✅ 客户端 B7 命中后 → backend 写 `phrase_block_uses` 表
- ✅ session 创建时聚合最近 8 个 hit block → 写入 `system.prompt.injected` 帧
- ✅ LLM 上下文带「最近用户命中过的话术块」section
- ✅ `total_uses` 字段异步聚合(避免同步写入热点)

**验收用例**(来自评估集 #28):
- 同一用户连续 3 次命中「review feedback」intent → LLM 在第 4 次回复时引用该话术
- 命中聚合 P95 ≤ 200ms

### 2.3 B25 F3 收藏置顶(PATCH 拆分)

**API 契约变更**:

```
PATCH /api/v1/corpus/:corpus_id
Content-Type: application/json

# 仅置顶
{ "pin": true }
# 仅收藏
{ "favorite": true }
# 取消置顶
{ "pin": false }
# 取消收藏
{ "favorite": false }
```

**deprecation 处理**:
- ✅ 旧 `POST /api/v1/corpus/:corpus_id/favorite` 标记 `@deprecated`,返回 deprecation header
- ✅ V1.5 移除(本期仅 deprecation,document 不删 endpoint)
- ✅ OpenAPI 同步(`api/openapi-v1.yaml` 手动编辑,W4 接入 oapi-codegen 自动生成)

### 2.4 B22 闪测 + A4 隐私删除

**仓库新增代码**:

```
services/
├── drill/
│   ├── round_builder.go             # 从调度队列拉 10 题
│   ├── judge.go                     # POST /drill/judge(Ark Mini 判定)
│   ├── sm2_scheduler.go             # D-4 简化 SM-2 调度
│   └── daily_picker.go              # 每日一读批处理 04:00 UTC
├── privacy/
│   ├── soft_delete.go               # DELETE /account/data + tombstones 表
│   ├── export.go                    # POST /account/export(D-API-2)
│   └── undelete.go                  # POST /internal/v1/support/undelete-user
└── migrations/
    ├── 0011_create_drill_records.sql                # 已就绪
    └── 0012_alter_privacy_soft_delete.sql           # W3 实施时补
```

**必须可工作的能力**:
- ✅ `GET /api/v1/drill/round` 返回 10 题 + `roundID`
- ✅ `POST /api/v1/drill/judge` Ark Mini 判定,P90 ≤ 1.5s
- ✅ 简化 SM-2 nextDueAt 计算正确
- ✅ `DELETE /api/v1/account/data` 软删除用户 + tombstones 表写入
- ✅ `POST /api/v1/account/export` 生成 JSON 导出
- ✅ `POST /internal/v1/support/undelete-user` 客服找回入口

**D-3 / D-4 验收**:
- ✅ Ark Mini 判定 P90 ≤ 1.5s
- ✅ SM-2 调度 nextDueAt 计算正确(单元测试覆盖)
- ✅ 闪测 round 跨 session 持久化(用户升级/降级不丢进度)

### 2.5 Backend 端到端冒烟

W3 末必须跑通的端到端测试脚本(放 `scripts/e2e/w3_smoke.sh`):

```bash
# 1. 注册 + 登录
TOKEN=$(curl -s -X POST /api/v1/auth/register | jq -r .token)

# 2. 创建会话 + 流式对话
WS_CONNECT /ws?token=$TOKEN
WS_SEND client.session.start
WS_RECEIVE ai.session.start  # 验证 WSS V2.0 协议

# 3. B7 命中注入
WS_SEND client.audio.start
WS_SEND client.turn.start
WS_RECEIVE ai.text.start + system.prompt.injected 含 RecentHits
WS_RECEIVE ai.tts.start + ai.tts.audio (Opus 流式)

# 4. 收藏置顶
curl -X PATCH /corpus/abc -d '{"pin": true}'

# 5. 闪测
curl -X GET /drill/round
curl -X POST /drill/judge -d '{...}'

# 6. 一键删除
curl -X DELETE /account/data -d '{"confirm": true}'
```

---

## 三、iOS 必须达成的产出

### 3.1 I20 Prompt 工程师接入

**新增/修改文件**:

```
Shared/FluentWorkCore/
├── SpeechSession/
│   ├── SpeechSessionMachine.swift     # + turn 超时兜底
│   └── TurnOutcome.swift              # 🆕 枚举
├── Prompt/
│   └── SystemPromptBuilder.swift      # 🆕 B7 hits + userLevel 注入
└── SpeechSession/SpeechSessionState.swift  # 已支持新状态
```

**必须可工作的能力**:
- ✅ 60s turn 超时 → 主动发 `client.turn.abort` 帧
- ✅ outcome 枚举 4 种:ok / timeout / userAbandoned / error
- ✅ Prompt 模板 V2.0 含「最近 8 个 hit block」+ 用户水平
- ✅ 埋点:`turn.timeout` + `turn.outcome` event

**38_ §1.2 缺陷修复验证**:
- ✅ 缺陷 #1:`outcome=ok` 误标修复(已 live 验证)
- ✅ 缺陷 #2:iOS 端不再发送矛盾 outcome(已 live 验证)

### 3.2 I21 状态机 7 状态

**新增/修改文件**:

```
Shared/FluentWorkCore/
└── SpeechSession/
    ├── SpeechSessionState.swift       # 5→7 状态
    └── SpeechSessionMachine.swift     # + isValidTransition(8 种合法)
```

**必须可工作的能力**:
- ✅ 状态枚举扩为 7 个:新增 `waitingForAIAnswer` + `waitingForEvaluation`
- ✅ 8 种合法转换规则完整 + 拒绝非法转换
- ✅ UI 绑定:`waitingFor*` 状态显示「AI 思考中…」「正在评价本次表现…」
- ✅ 与 I20 联动:60s 超时 → waitingForAIAnswer
- ✅ 状态转换埋点 `state.transition` 上报

### 3.3 I15 说的房间 TTS 播放

**新增/修改文件**:

```
Shared/FluentWorkCore/
├── Audio/
│   └── TTSPlayer.swift                # 🆕 AVAudioEngine + Opus 解码
├── Networking/
│   └── WSFrameDispatcher.swift        # + ai.tts.* 帧处理
└── Settings/
    └── VoiceSettingsView.swift        # 🆕 4 个音色选择 UI
```

**必须可工作的能力**:
- ✅ 收到 `ai.tts.start` 帧 → 准备 Opus 解码 + 清空旧 buffer
- ✅ 收到 `ai.tts.audio` 帧 → 流式 enqueue + 播放
- ✅ 收到 `ai.tts.end` 帧 → 标记结束(不立即停)
- ✅ 用户按住 HoldToSpeakButton → interrupt TTS ≤ 100ms + 切 listening 状态
- ✅ 4 个 D-2 音色 UI + UserDefaults 持久化

**性能指标**:
| 指标 | 目标 | 实测 |
|---|---|---|
| 首字延迟 P90 | ≤ 400ms | 待 W3 末验收 |
| 打断延迟 P99 | ≤ 100ms | (实测) |
| 多 session 并发 | 不支持 | 单元测试 mock 覆盖 |
| 蓝牙耳机断开 | 自动切换扬声器 | 兼容测试 |

### 3.4 iOS 端到端冒烟

W3 末必须跑通的 UI 测试脚本(`Tests/FluentWorkCoreTests/E2EW3Tests.swift`):

```swift
// 1. 启动会话,等待 60s
let session = await startSession()
try await Task.sleep(nanoseconds: 60_000_000_000) // 60s
// 验证:状态机进入 waitingForAIAnswer,abort 帧已发

// 2. TTS 播放
let tts = await startTurn()
try await Task.sleep(nanoseconds: 2_000_000_000)
await assertAudioPlayed(tts)

// 3. 用户打断
try await tapHoldToSpeakButton()
await assertTTSStopped()

// 4. 状态机完整链路
let states: [SpeechSessionState] = [
    .listening, .processing, .speaking, .waitingForAIAnswer, .ended
]
await assertStateMachineSequence(states)
```

---

## 四、Infra 必须达成的产出

| 项 | 内容 | Owner |
|---|---|---|
| 监控告警 | voicegateway + ai-worker 关键路径 | infra |
| TOS bucket | C-3(B20 每日一读前置) | infra |
| 灾备 region | 跨区域复制配置(W4 压测用) | infra |
| schema 同步 | 跨仓 schema diff 每日跑 | infra |

---

## 五、文档同步产出(每个 Issue CLOSED 时必做)

### 5.1 必更新的文档清单

| 文档 | 更新内容 | 触发条件 |
|---|---|---|
| `55_` W3 启动包 | 行状态 `🟢 已建仓 + #XXX` → `🟢 CLOSED ✅` | 每个 Issue CLOSED |
| `60_` iOS W3-W4 启动包 | 同上 | 同上 |
| `73_` 蓝图 §五 | 累计 CLOSED 数 + 燃尽图 | 每周五 |
| `53_` §五之三 F-6 | 状态列加 `CLOSED ✅` + 闭合日期 | 每个 Issue CLOSED |
| `48_` REST 契约 | B25 的 PATCH 拆分节确认无变更 | B25 CLOSED |
| `49_` 数据库迁移 | 0012 迁移 indexed 状态 | B22 CLOSED |
| `50_` 全链路架构 | TTS 流式 + B7 注入链路图更新 | B17 + B19 CLOSED |
| `32_` iOS 设计 | 状态机图(7 状态)更新 | I21 CLOSED |
| `38_` I20 收口 | 4 个缺陷修复状态 | I20 CLOSED |
| `74_` 凭证与 P0 修复日志 | B17/B22 凭证使用情况 | B17/B22 CLOSED |

### 5.2 OpenAPI 同步

```bash
# 手动编辑 api/openapi-v1.yaml
openapi: 3.1.0
info:
  title: FluentWork API
  version: v1.0-w3
paths:
  /corpus/{corpus_id}:
    patch:
      summary: Update corpus item (pin/favorite)  # 🆕 B25
      ...
  /drill/round:
    get: { ... }  # 🆕 B22
  /drill/judge:
    post: { ... }  # 🆕 B22
  /account/data:
    delete: { ... }  # 🆕 B22
  /account/export:
    post: { ... }  # 🆕 B22
```

> ⚠️ W3 暂手动编辑,W4 接入 `oapi-codegen` 自动生成。

---

## 六、跨仓集成验收(必须真实跑通)

### 6.1 端到端冒烟脚本

W3 末必须有一个真实跑通的脚本:

```bash
# /scripts/e2e/w3-smoke.sh
#!/bin/bash
set -e

echo "[1/7] Register & login"
TOKEN=$(curl -s -X POST http://localhost:8080/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"w3-test@fluentwork.dev","password":"test"}' | jq -r .token)

echo "[2/7] Create session via WSS"
SESSION_ID=$(echo "client.session.start" | \
  websocat ws://localhost:8080/ws?token=$TOKEN | jq -r .session_id)

echo "[3/7] Inject B7 hit → trigger LLM with prompt"
echo "client.turn.start
{\"hit\":\"intro_self\",\"chunk\":\"Hi, I'm a PM\",\"intent\":\"自我介绍\"}" | \
  websocat ws://localhost:8080/ws?token=$TOKEN

echo "[4/7] Receive TTS audio stream"
echo "client.turn.start
{\"audio\":\"...base64...\"}" | \
  websocat ws://localhost:8080/ws?token=$TOKEN | grep "ai.tts.audio"

echo "[5/7] Pin corpus item"
curl -s -X PATCH http://localhost:8080/api/v1/corpus/abc \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"pin":true}'

echo "[6/7] Get drill round"
curl -s http://localhost:8080/api/v1/drill/round \
  -H "Authorization: Bearer $TOKEN"

echo "[7/7] Delete account (A4)"
curl -s -X DELETE http://localhost:8080/api/v1/account/data \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"confirm":true}'

echo "✅ W3 smoke test passed"
```

### 6.2 iOS 端到端冒烟(XCUITest)

`Tests/UITests/W3SmokeTests.swift`:

```swift
class W3SmokeTests: XCTestCase {
    func testVoiceFlowEnd2End() throws {
        let app = XCUIApplication()
        app.launch()
        
        // 1. 启动会话
        app.buttons["StartSession"].tap()
        
        // 2. 按住 HoldToSpeakButton 3s
        let button = app.buttons["HoldToSpeak"]
        button.press(forDuration: 3)
        
        // 3. 等待 AI TTS 播放(60s 内应听到声音)
        let playing = app.staticTexts["AI Speaking"]
        XCTAssertTrue(playing.waitForExistence(timeout: 5))
        
        // 4. 用户打断
        button.press(forDuration: 1)
        XCTAssertTrue(app.staticTexts["Listening"].waitForExistence(timeout: 1))
        
        // 5. 60s 后验证兜底
        sleep(60)
        XCTAssertTrue(app.staticTexts["AI Thinking"].exists)
    }
}
```

---

## 七、性能与质量验收标准

### 7.1 Backend 性能

| 指标 | 目标 | 验收方法 |
|---|---|---|
| voicegateway 首字 P90 | ≤ 400ms | `/scripts/perf/tts-bench.go` |
| B19 命中聚合 P95 | ≤ 200ms | `/scripts/perf/hits-bench.go` |
| 闪测判定 P90 | ≤ 1.5s | `/scripts/perf/drill-bench.go` |
| 一键删除 P95 | ≤ 5s(含导出) | 手动测 |
| 单 service p99 | ≤ 1s | Prometheus |

### 7.2 iOS 性能

| 指标 | 目标 | 验收方法 |
|---|---|---|
| TTS 首字 P90 | ≤ 400ms | `TTSPlayerTests` |
| 打断延迟 P99 | ≤ 100ms | `SpeakingRoomFeatureTests` |
| 状态机转换开销 | ≤ 10ms | `SpeechSessionStateMachineTests` |
| carousel 滑动 FPS | ≥ 60 | Instruments |

### 7.3 单元测试覆盖率

| 仓库 | 目标 | 实际 |
|---|---|---|
| backend Go | ≥ 80% | (CI 卡门禁) |
| iOS Swift | ≥ 70% | (CI 卡门禁) |
| e2e 冒烟 | 100% 通过 | 跨仓跑通 |

---

## 八、Stakeholder 演示与评审

### 8.1 W3 末周五 16:00 收口会

**议程**(30 min):

| 时长 | 内容 | Owner |
|---|---|---|
| 5 min | W3 末完成度(8 个一级 Issue 全部 CLOSED) | 后端 TL |
| 10 min | 端到端冒烟演示 | 后端 + iOS |
| 5 min | 性能数据(首字 P90 / 判定 P90 / 打断 P99) | infra |
| 5 min | 跨仓问题 / 阻塞 | 全员 |
| 5 min | W4 启动安排 + B16 进度 | 后端 TL |

### 8.2 内测材料

W3 末提交:
- [ ] 录制 60s 端到端真机演示视频(中英文各一段)
- [ ] 5 张关键 UI 截图(创建会话 / TTS 播放 / 闪测 / 收藏 / 删除)
- [ ] 性能 benchmark 报告(`/docs/perf/w3-report.md`)
- [ ] 已知问题清单(acceptable for W3)

### 8.3 验收 Checklist(发给 Reviewer)

- [ ] **代码层**:7 个一级 Issue 全部 CLOSED,PR merged
- [ ] **API 层**:OpenAPI YAML 已更新,Postman collection 已更新
- [ ] **DB 层**:0012 迁移已合入,down.sql 可用
- [ ] **测试层**:单元 + 集成 + e2e 三层 PASS
- [ ] **文档层**:10 份主文档同步更新(§五)
- [ ] **Demo**:周五 16:00 端到端演示通过
- [ ] **Performance**:首字 P90 ≤ 400ms 实测
- [ ] **Deployed**:staging 环境跑通,production 灰度 5% 流量

---

## 九、W3 → W4 交接清单

### 9.1 W3 末交付给 W4 的前置

| 前置 | 来源 | 状态 |
|---|---|---|
| B17 TTS 端到端跑通 | W3 | ⏸ 待 W3 末 |
| I20 TurnOutcome 枚举定义 | W3 | ⏸ 待 W3 末 |
| I21 7 状态枚举 | W3 | ⏸ 待 W3 末 |
| B25 PATCH 拆分契约 | W3 | ⏸ 待 W3 末 |
| 0012 软删除迁移 | W3 (B22) | ⏸ 待 W3 末 |
| WSS V2.0 帧协议冻结版 | DC-3 | ⏸ 9/15(W3 中期) |

### 9.2 W4 启动包立即可建仓

- **B21** 素材模块 → iOS **I14** 创建练习弹层
- **B18** review eval → iOS **I16** 完整转录浮层
- **B23** 话题卡生成 → iOS **I18** 话题卡 UI
- **B24** 历史回顾 API → iOS **I19** 历史回顾列表

### 9.3 W4 决策与凭证依赖

| 依赖 | 截止 | Owner |
|---|---|---|
| C-1 火山 Ark Mini | ✅ 9/10 | 商务 + 后端 TL |
| C-2 火山独立 TTS | ✅ 9/10 | 商务 + 后端 TL |
| B16 AIOrchestrator 启动包 | 9/12 | 后端 TL |
| #28 评估集完成 | 9/13 | 后端 AI 组 |
| WSS V2.0 冻结版 | 9/15(W3 中期) | 后端 TL + iOS Lead |

---

## 十、未决项明示

| 项 | 截止 | 影响 | 兜底 |
|---|---|---|---|
| C-1 / C-2 凭证延期 | 9/10 | W3 后端 4 Issue 顺延 5-7 天 | NFR 测试账号先用 |
| B16 启动包延期 | 9/12 | W4-W5 部分 Issue 用 stub | 9/9 提前 commit 骨架 |
| WSS V2.0 冻结延期 | 9/15 | iOS / backend 联调延期 | 9/15 必须冻结 |
| OpenCodeReview pre-commit | W3 末 | 合规 | 后端 TL 跟进 |

---

## 十一、与 README §四 + 53_ §五之三 F-6 对账

| 53_ § | 本文档对应 | 状态 |
|---|---|---|
| F-6 W3 末预期 | §一 + §二 + §三 | ✅ 一致 |
| 启动包就绪率 | §一 | ✅ 7/7 W3 Issue 启动包就绪 |
| 凭证到位 | §十 | ✅ 9/10 前到位 |
| 端到端冒烟 | §六 | ✅ 跨仓脚本就绪 |

---

*文档版本:V1.0(2026-09-06 晚场首次落库)*
*上游依据:`73_` V1.0 / `55_` V1.1 / `60_` V1.0 / `48_` / `49_` / `74_` 凭证日志*
*下游交付:W3 周五 16:00 收口会 + W4 启动包解锁 + 端到端演示视频*
*同步节奏:W3 末完成时更新 V1.1,W4 启动后归档到 `60_评审与复盘/`*
