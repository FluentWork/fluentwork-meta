# 问题总清单 · iOS 架构轴

> 本文件是 [`问题总清单.md`](./问题总清单.md) 的**第 3 节**（按章节拆分，2026-09-25）。
> **唯一入口是索引**；本文件只承载这一条轴的内容。
> **编号**：本节所有 `S0-x`/`S1-x`/`S2-x` **写作 `iOS-S…`**（与 backend 的 S 号撞车，故加前缀）。
> **内容逐字来自** `fluentwork-ios/docs/80_架构分析/05_问题清单与建议.md`
> —— 该文件已于 2026-09-25 随整个 `docs/` 删除（提交 `80c8209`）。
> ⚠️ **本文件因此成了这些条目唯一的载体**，不再是「收敛件」。
> 原文件是 `80_架构分析/01`–`04` 四章的收敛，**那四章同样已删除**；
> 下文形如「`01`–`04` §x.y」的行号引用**不再可解析**，只能当历史标注读。


> 基线 `2aa8795`。本文件是 `01`–`04` 的收敛：**不引入新结论**，只把四章里已经带证据的条目排成一张可执行的表。
> 每条都指向原章的行号，可当场复核。

### 0. 这份清单怎么读

**分级只按一件事**：不做会怎样。

| 级 | 含义 | 判据 |
|---|---|---|
| **S0** | 不做就继续带着已知风险跑 | 已在真机出过事故，或需要一个只有人能给的决定 |
| **S1** | 不做会在下一次同类缺陷上再花一遍时间 | 结构上「看起来被守住了，实际上没有东西会红」 |
| **S2** | 不做只是让下一个人多读一会儿 | 重复、命名、注释、死代码 |

**「需决策」列**是这份清单的重点。S1/S2 我可以直接做；S0 里的每一条都卡在一个本仓无法单独回答的问题上。

**确定性沿用系列约定**：【实测】有命令输出；【读码】从源码读出；【推断】可能另有解释。

---

### 1. S0 — 需要先有决定

这四条**不是我不确定该怎么做，是它们的答案不在本仓**。

| # | 条目 | 卡在谁那里 | 证据 | 确定性 |
|---|---|---|---|---|
| iOS-S0-1 | **Stage 3：二进制帧带 `turn_id`** —— **已决：排期，作为下一项主任务**；契约草案见 `70_/25_` §3 | 后端 | 后端 `voiceproto/frames.go:195-203` 仍是 `[4B seq][payload]`；做了就同时关掉 D7 与 D2 | 【实测】 |
| iOS-S0-2 | **D5 解码失败策略** —— **已决：选项 2**（`voice_id`/`sample_rate`/`codec` 改 `decodeIfPresent`，`turn_id` 保持必需）；理由见 `70_/25_` §2.3 | 产品 | `70_/10_` D5 | 【读码】 |
| iOS-S0-3 | **真机验证 barge-in 与断线重连**（需一次 >5 秒的回复） | 设备与时间 | `70_/08_` §3；两次真机静音事故都是单测全绿时发生的 | 【实测】 |
| iOS-S0-4 | **两个设计文档分支的处置** —— **已决：不并进 main**（`70_/25_` §4.2） | 你 | `70_/11_` §7 | 【读码】 |
| iOS-S0-5 | **远端 `main` 的分支保护 vs `AGENTS.md`**：远端要求 PR + 3 项状态检查，而 `AGENTS.md` 写着「develop on `main`, no PRs」；每次推送都报 `Bypassed rule violations` | 仓库管理员（**我执行不了**） | `70_/25_` §5 | 【实测】 |

**S0 已清空三条（2026-09-23，`70_/25_`）**：iOS-S0-1 / iOS-S0-2 / iOS-S0-4 都有了决定，**只剩 iOS-S0-3**（真机验证）仍是设备与时间问题。新增的 iOS-S0-5 是一条**流程**冲突，不是代码问题，需要仓库管理员动手。

**iOS-S0-1 值得单独说一句**：它是这份清单里**唯一一条一条改动能同时关掉两个缺陷**的（D7 归属推断 + D2 序号水印的残留形状），也是唯一一条**跨仓**的。本仓已经为它留好了位置——`TTSPlaybackCoordinator` 的归属判定已经是「按 turn 轴」的，只是 turn 号目前靠控制帧时序推出来。

**iOS-S0-3 不是「补测」**：`03` §4.5 记的事实是，这个仓的测试层级**结构上测不到那类问题**（真机静音、真机 barge-in）。所以它不能靠加单测解决，只能排时间。

---

### 2. S1 — 结构上该补的「防线」

共同形状：**判据存在，但没有任何东西会因为它失效而变红。**

| # | 条目 | 动作 | 风险 | 价值 | 证据 |
|---|---|---|---|---|---|
| iOS-S1-1 | ~~`pcmBuffer` / `isCapturingSpeech` 跨会话存活，安全性依赖 `removeAll()` 的调用顺序~~ **已关闭（前提被推翻）**：零读点，是死代码不是不变量，已删除（`70_/22_`，D12） | ~~收进类型，**或**加一条测试~~ 已删除 | — | — | `03` §4.4 的「前提已被推翻」 |
| iOS-S1-2 | 11 份等待助手（`waitUntil` ×8 + `waitForPhase` ×2 + `waitForProcessingStage` ×1）+ 8 份错误类型，已漂移 5 类 | 抽成一份测试支撑 | 低 | **高** | `04` §3.1 |
| iOS-S1-3 | 超时值的理由只有一份副本（`SpeechSessionMiddlewareTests.swift:2155-2168`），而 `SpeakingRoomSessionWiringTests` 还有 65 处 1 秒 | 把那段注释提成共享，再据此重定 | 低 | **高** | `04` §3.2 |
| iOS-S1-4 | ~~`audioEventPump`（上行，209 行）未纳入路由表（D9）~~ **推荐改法不成立**（见下）；真问题改为「文件 1735 行的**切缝选择**」 | ~~抽成路由表~~ → 选一条内聚切缝（候选：6 个纯 `timings.mark` 的采集诊断 / 3 条会抛的上行路径 / `makeTransportEventRouter` 341 行） | 低 | 中 | `03` §4.3 + `70_/23_` §10 |
| iOS-S1-5 | 无端到端测试：`Integration/` 只有 51 行 | 补一条进程内真 socket → 中间件 → store | 中 | **高** | `04` §3.3 |
| iOS-S1-6 | DI 是「可注入」而非「必须注入」：**10 处** `?? Container.shared` | 去掉默认值，让 container 必传 | 低 | 中 | `02` §4.1 |
| iOS-S1-7 | D8：音频路径每帧两次 actor 跳转，而 `TransportEventRouter` 自己的注释反对其中一次 | **先量基线**，再决定是否合并 | 中 | 中 | `03` §4.2 |
| iOS-S1-8 | `FWTryCatch` 是崩溃面的唯一防线，5 个调用点 | 查真机日志有没有触发过 | 低 | 中 | `01` §4.4 |
| iOS-S1-9 | ~~**8 个采集诊断的 mark 名没有任何测试钉住**~~ **已关闭（2026-09-23）**：**数量更正为 11 条**（原文只数了 `.capture*` 一族），已全部钉住；生产代码零改动 | ~~用 `CapturingTracker` 驱动每个事件并断言 `timing_<name>`~~ 已按此实施，并另用**两次变异验证**证明这条测试会咬 | — | — | `70_/23_` §10 → `70_/24_` |
| iOS-S1-10 | **`AVAudioSession` 有两个实现点，而其中一个是绕过 owner 的**：`LiveAudioEngine` 走 `sessionManager.configure(for: .fullDuplex)`（→ `.playAndRecord`/`.voiceChat`，`LiveAudioEngine.swift:459`/`:661`），而 `DailyReadAudioPlayer` **不注入 `AudioSessionManaging`**，直接拿 `AVAudioSession.sharedInstance()` 并把它切成 `.playback`（`:117`/`:156`/`:166`）——那会拆掉 input route 并**静默停掉正在运行的 `AVAudioEngine`**（一行我们的代码都不执行，所以 `isRunning` 变 false 时没有任何栈可读）。唯一看起来能判「谁在占用」的 `DefaultAudioSessionManager.isActive` 是**永不取假值的判据**（它读的 `active` 只在 `configure` 里置 `true`，而 `pause()` 在生产里没有调用者，只在测试里被调） | 让 `DailyReadAudioPlayer` 也走 `AudioSessionManaging`，或把「谁在占用」变成可查询的真值 | 中 | **高** | `70_/27_`（2026-09-24 真机事故） |

~~**iOS-S1-1 是本清单里最像「定时炸弹」的一条**：它不是性能也不是风格，是**一个没有测试保护的不变量**。`03` §4.4 的结论是——如果哪天有人调整了 `removeAll()` 的顺序，**没有任何测试会红**。~~

**iOS-S1-1 的更正（2026-09-22，`70_/22_`）**：它**不是**定时炸弹，因为**引信不存在**。查证 9 个出现位置的读写方向后：`pcmBuffer` **零读点**，`isCapturingSpeech` 唯一一次被读只是决定要不要往这个死 buffer 里写。所以「调整 `removeAll()` 的顺序会串上一场的音频」这个后果**不可能发生**——串音频需要有人读到它。**已删除**（D12）。

**这条更正本身值得记住，因为它演示了这个系列最容易犯的错**：上面那句「本清单里最像定时炸弹的一条」写得很自信，但它的全部依据是「有一个跨会话的 buffer，且它的安全性靠调用顺序」。**这个描述从头到尾都成立——只是描述的对象是死的。** 前提未验证时，分析越细致，错得越像对的。这与 `70_/13_`（D5）推翻 D5 自身立论、`70_/16_`/`17_` 的「判据从不取真值」是同一族：**先验证前提，再决定动作。**

**iOS-S1-3 的形状值得记住**：2026-09-12 那节课（1 秒超时不是延迟断言）在一个文件里被认真学到了、写下了理由、点名了两次 CI 失败——然后**没有传到那个有 65 处同类调用的文件**。这不是「没人想过」，是「想过的地方和要用的地方不是同一个地方」。

**iOS-S1-7 排在 S1 而不是 S0**：因为 `03` §4.2 的结论是**先测量再改**。没有延迟基线就合并跳转，是拿一个未知换另一个未知。

**iOS-S1-4 的更正（2026-09-23，`70_/23_` §10）**：D9 的推荐改法（「抽成路由表，复用 `20_` 的手法」）**在本仓不成立**，已同步改 `70_/10_` 的 D9 条目。四条证据：

1. **`AudioEngineEvent` 没有 discriminator** —— 纯 Swift enum，14 个 case 只有关联值。**建不出字典表。**
2. **建了反而更不安全** —— 下行的 `controlHandlers` 是 `[WSControlFrameType: ControlFrameHandler]`（字符串键），**漏键是运行时静默落 fallback**；而上行 switch **没有 `default:`**，新增 case 是**编译错误**。换字典 = 把编译期保证换成运行时缺口。
3. **控制流表达不了** —— 上行 switch 的 `continue`（2 处）与 `return nil`（4 处）是跳出 switch 进入外层循环的转移；`TransportEventRouter.route` 返回 `Void` 的理由（「路由不产生决定」）**对上行是假的**。
4. **真问题是「文件 1735 行」**，切缝选择是设计决定，不该按对称性选。

**iOS-S1-9 是查 iOS-S1-4 时撞出来的，而它比 iOS-S1-4 本身更值钱**：那 8 个 mark 名（**数量更正为 11，见下**）是 `08_` §3 尚未进行的真机验证**唯一计划中的证据来源**（`captureKick` 的注释写着「'the fix did not work' and 'the fix was never installed' were indistinguishable — which is the failure mode this project keeps paying for」），而**没有任何测试钉住它们**——一次改名或拼写错误就能静默移掉整条诊断链。这与 `70_/16_`/`17_`（判据从不取真值）、`70_/19_` §6.3（判据被复制进替身）、`70_/22_`（保护对象不存在）同族，是本仓第四次撞到「看起来有保护、实际没有」。**它只改测试就能修**，是本清单里性价比最高的一条。

**iOS-S1-9 已关闭（2026-09-23，`70_/24_`）**：**11 条**埋点名（不是原文的 8 条，更正见 `80_/README` 自校「第五处」）已由 `captureDiagnosticsKeepTheirMarkNamesAndPropertyKeys` 全部钉住，生产代码零改动。

**但本条的交付物不是「加了一条测试」，而是「证明了这条测试会咬」。**「钉住既有行为」的票有一条特有的失效模式：**它可能永远绿**——而一条永远绿的守卫比没有守卫更糟，因为它会让下一个人以为这件事被管住了。所以 `24_` §6.2 用**两次变异验证**（改掉一个埋点名 / 删掉一个属性键）确认它会红，且失败信息指名道姓（哪条埋点、缺哪个键）。这与 `70_/22_` §6.2 问的「守卫守的是**效果**还是**机制**」是同一类追问——**先验证前提，再决定动作**的又一种形态：这次要验证的是守卫本身。

**iOS-S1-10 是 2026-09-24 真机事故的残余（`70_/27_`）**：练习会话卡在 `.connecting` 直到 10s 看门狗失败，而**一行我们的代码都没执行**——`DailyReadAudioPlayer` 为了锁屏继续播报，把共享的 `AVAudioSession` 切成 `.playback`，拆掉了 input route，`AVAudioEngine` 于是自己停了。`27_` 的修法是**局部的**（切之前先看真实 category，`.playAndRecord` 时不动），因为 `AVPlayer` 在 `.playAndRecord` 下照样能放——**它只是不需要赢这场争夺，只需要不替对方输掉**。

**但结构问题没关**，所以留在 S1——而且它的准确形状是**「有 owner，但有人绕过它」**，不是「没有 owner」：本仓**已经**有 `AudioSessionManaging` / `DefaultAudioSessionManager` 这个归属点（`LiveAudioEngine` 老实地走它，`FeatureFlags.swift:21` 的注释也把它当作 `.playAndRecord`/`.voiceChat` 的设定者），**而 `DailyReadAudioPlayer` 根本没注入它**，直接拿 `AVAudioSession.sharedInstance()` 自己配。于是同一个语义有了两个实现点——这是 `AGENTS.md` R2 那一类问题。

唯一看起来像答案的 `DefaultAudioSessionManager.isActive` 还是个**永不取假值的判据**：它读的 `active` 只在 `configure()` 里置 `true`，而 `pause()` 在生产里无调用者，所以它会永远报「房间还活着」——照它做判断会**静默禁掉** Daily Read 的音频。`27_` 因此选择读**真实 category**而不是读那个标志位。这与 `70_/16_`/`17_` 的「判据从不取真值」是同一个形状，只是这次的判据**已经存在于生产代码里**。

> **顺带一条方法论**：iOS-S1-10 的第一版我写成「**没有任何东西拥有它**」——那是在读 `AudioSessionManaging.swift` **之前**下的判断。读过之后形状变了：owner 在，绕道才是问题。**先说结论再补证据的写法在这里会写错**，所以本条按「先读事实再下判断」重写过一次。

**iOS-S1-10 与 iOS-S1-3 的教训是同一条**：`27_` 加的三处诊断（`describeSession()`、`captureArmed` 的 `session` 键、`session_interrupted_by_system` 埋点）都不是「顺手加的日志」——**在真机上，一行都不执行的失败只能靠这类痕迹归因**，而本仓已经付过两次「两侧单测全绿而真机静音」的代价（`70_/08_` §3）。

---

### 3. S2 — 卫生

| # | 条目 | 证据 |
|---|---|---|
| iOS-S2-1 | 同名替身跨文件重复：`StubCorpusClient` ×3、`StubDailyReadAPIClient` ×3 | `04` §3.4 |
| iOS-S2-2 | `AppBootstrapMiddleware.swift`（836 行）定义 **4 个**无关 middleware，文件名与内容不符 | `02` §4.2 |
| iOS-S2-3 | `appCrossCuttingReducer` 是没被命名的「第二套 reducer」，78 行 | `02` §4.3 |
| iOS-S2-4 | 手写的去重/闸门有 5 个，而框架提供 `.debounce`（全仓用了 **0** 次） | `02` §4.4 |
| iOS-S2-5 | 三种隔离策略并存（17 actor / 5 `@MainActor` / 11 锁），无统一规则 | `02` §4.5 |
| iOS-S2-6 | 7 个 `.shared` 进程单例，没有逐个论证为什么必须是单例 | `02` §4.1 |
| iOS-S2-7 | Moya 钉在个人 fork 的 `master` 分支，不是 tag/revision | `01` §4.2 |
| iOS-S2-8 | `Modules/` 是空目录（只有 `.gitkeep`） | `01` §4.1 |
| iOS-S2-9 | ~~`LiveAudioEngine.deinit` 无条件 `removeTap`~~ **已关闭（2026-09-25，`70_/28_`）—— 且它不该留在 S2**：实测**全量 `swift test` 期间输入设备被打开 28 次**（`deinit` 每次销毁都求值 `engine.inputNode`，而那一步就是打开设备）。不是卫生问题，是「测试在调用真机麦克风」 | 已按 `70_/16_` §7.1 给出的判据修（只移除自己装过的 tap） | `70_/16_` §7.1 → `70_/28_` |
| iOS-S2-10 | `sendInterrupt()` 吞掉 `try?` | `70_/19_` §7 |
| iOS-S2-11 | `handshake` 是 iOS 侧死面（`Shared/` 从不构造它） | `70_/20_` §7 |
| iOS-S2-12 | 丢弃的**计数**没有上报 | `70_/21_` §7 |

**iOS-S2-4 值得解释一下**：`02` §4.4 统计出 5 处手写去重、11 处 `OSAllocatedUnfairLock`，而 TGReduxKit 的 Effect 提供了 `.debounce` 且**全仓 0 次使用**。这不必然是错的——手写版有时更精确——但**没有任何地方写下来为什么**。

**iOS-S2-9 的分类是错的，而它错的方式值得单独记（2026-09-25，`70_/28_`）。** 本条原先只写「`deinit` 无条件 `removeTap`」，归到 S2「卫生」。实测之后它不是卫生问题：`engine.inputNode` 的**求值本身**就是打开输入设备的那一步（`LiveAudioEngine` 自己的注释写着「Querying the input format forces lazy node creation」），而全量 `swift test` 期间这条 `deinit` 跑了 **28 次** —— **那就是「测试在调用真机麦克风」**。分级只按「不做会怎样」，而「测试持续打开音频输入设备」的后果显然不止「让下一个人多读一会儿」。

**更要紧的是 `70_/16_` §7.1 为什么没改它。** 那条诊断一字不差，判据也给对了，但当时的理由里有一句：

> §4 显示 39→0 之后这条路径在测试里已经不再发生

**这句不成立。** 39→0 说的是**容器**路径（接线测试不再解析出真引擎）；而 `LiveAudioEngineTests` 是**直接构造**真引擎 —— 它是引擎的测试，本来就该构造。那条路径**从来没被数过**。

| 票 | 形状 |
|---|---|
| `70_/18_` | 把「少了一道防线」当成「留下的那道是对的」 |
| **`70_/28_`** | **把「一条路径堵上了」当成「这个洞堵上了」** |

两次的错法一样：**用一个局部的事实去支撑一个全局的结论。** 判据：**报数的时候要问「我数的是哪条路径」** —— 一个数字只对它被采集的那个集合成立。

**顺带关掉了一处 flaky**：那 28 次硬件 I/O 会把进程卡住，使 `24_` 那道「等泵处理完」的屏障有时要 1.5–3.4s，而预算只有 2s。去掉设备访问后，全量测试从 8.1–8.7s（且抖）变成 **6.17s（±50ms）**，连跑 6 次全绿。**所以「屏障预算不够」是症状，不是根因** —— 这一点只有把设备访问去掉之后才看得出来。

---

### 4. 反过来：这些看起来是问题，但**不要改**

写下来是为了防止下一个人（包括我）把它们当缺陷修掉。

| 看起来像 | 实际是 | 证据 |
|---|---|---|
| `TransportEventRouter` 是 `struct` 而不是 `actor`，要自己保证串行 | **刻意**。`actor` 在这里只提供调用方已保证的串行性，代价是每帧一次额外跳转 | `TransportEventRouter.swift:18-24` |
| 未知控制帧不回错误，只忽略 + 计数 | **与后端对称**，两侧都写了理由 | `URLSessionSocketTransport.swift:316-332` vs 后端 `handler_control.go:465-486` |
| `ai.audio.chunk` 收了但什么都不做 | **后端没有生产者**（`voiceproto/frames.go:21` 是唯一出现处）。只留痕，不播放 | `70_/20_` §2.2 |
| 丢弃埋点一轮只报一次 | 刻意。去重键是 (轮次, 原因)；改成 `markTurnOnce` 会给事件名加 `timing_` 前缀，**让真机核对表失效** | `70_/21_` §3.3–3.4 |
| `Tests/` 下只有 `FluentWorkCoreTests` 一个 target | 测试都在它下面按目录分；`UI/`、`Integration/` 是它的子目录 | `04` §1 |

---

### 5. 如果只做三件事

按「不做会怎样」排序，我选这三条：

1. **iOS-S0-1（Stage 3）** —— 唯一一条同时关掉 D7 与 D2 的改动，且本仓已经为它铺好了路。**已决：排期，作为下一项主任务**（2026-09-23，`70_/25_` §3）。
2. ~~**iOS-S1-1（`pcmBuffer` 的不变量）** —— 一个没有测试保护的不变量，且它在**音频路径**上（`AGENTS.md` High-Risk Path #1）。加测试的成本极低。~~ **本条已作废**：查证后发现 `pcmBuffer` 零读点，是死代码而非不变量，已删除（`70_/22_`，D12）。**保留下来的教训比这条建议本身更值钱**——原文的分析与建议都建立在一个未经验证的前提上（「它是活的」），见 `03` §4.4。
3. **iOS-S1-2 + iOS-S1-3（测试助手与超时理由）** —— 两条合起来是一次改动：把漂移的 11 份助手收敛成一份，同时让那段超时论证只有一个副本。这是本系列**最具体**的发现。

不选 iOS-S1-5（端到端测试）不是因为不重要，是因为它成本最高，且 iOS-S0-3 的真机验证能覆盖它的一部分价值。

---

### 6. 本轮已关闭的（不在上面的清单里）

`70_tts_wss_refactor/` 的 10 张票已经落地，这里不重复。

> ⚠️ 原索引 `fluentwork-ios/docs/70_tts_wss_refactor/README.md` 已随 iOS 的 `docs/` 删除，
> **下表是那 10 张票留下的唯一记录。**

| 票 | 提交 | 一句话 |
|---|---|---|
| D1 | `0436c27` | 首块音频的宣告必须晚于归属判定 |
| D2 | `79e0ffc` | 删掉传输层序号水印 |
| D3 | `bfc8fc2` | 一次 barge-in 只发一次 `control.interrupt` |
| D4 | `ddee3ae` | `.waitingUser` 下的 barge-in 必须停掉残留音频 |
| D5 | `bf266b6` | 致命解码失败要点名字段（**并推翻了 D5 自身的立论**） |
| D6 | `2aa8795` | 丢弃埋点按 (轮次, 原因) 去重 |
| D10 | `e9d9b4e` | 打断改用显式 `sendInterrupt()`，删掉 `__interrupt__` 哨兵 |
| D11 | `de19195` | 已知但无人接的控制帧必须留痕 |
| — | `5da29f9` | 测试进程不得构造真的音频引擎 |
| — | `a007cb6` | 测试进程里麦克风替身恒开 |
| D12 | `7ee195c` | 删掉 `audioEventPump` 里从未被读的跨会话音频缓冲（**立论被推翻**） |
| D14 | `35dba0f` | 引擎报一次 `.failed` 不再终止**进程级**的上行泵 |
| iOS-S1-9 | `—` | 11 条上行采集诊断埋点名有了测试钉住（**并更正 8 → 11**），生产代码零改动 |
| D15 | `—` | 3 处上行**发送**失败的 `return nil` 不再终止**进程级**的上行泵（**D14 的另两条路径**） |
| iOS-S1-10 的局部修复 | `—` | `DailyReadAudioPlayer` 不再无条件把共享 `AVAudioSession` 切成 `.playback`（那会静默停掉正在跑的 `AVAudioEngine`）；并补三处真机可归因诊断（`describeSession()` / `captureArmed` 的 `session` 键 / `session_interrupted_by_system` 埋点）——见 `70_/27_`。**结构问题（无人拥有 session）仍开着，见 iOS-S1-10。** |
| iOS-S2-9 | `—` | `LiveAudioEngine.deinit` 不再无条件碰输入节点（只在**自己装过 tap** 时摘），并新增第三个注入点 `removeCaptureTap`。全量测试打开输入设备 **28 → 0** 次；顺带关掉一处 flaky（8.1–8.7s → **6.17s**）——见 `70_/28_` |

**仍未修的是 D7 / D8 / D9**——三条都在本文件的 iOS-S0-1 与 S1 里。另加 **iOS-S1-10**（`AVAudioSession` 无人拥有）与 **iOS-S1-2 / iOS-S1-3**（测试助手与超时理由，见 §5 第 3 条）。

---

### 7. 与其它文档的关系

| 想知道 | 去哪 |
|---|---|
| ~~单条缺陷的根因与红输出~~ | **`docs/70_tts_wss_refactor/` 已随 `docs/` 删除** |
| ~~本仓的分层、DI、语音链路、测试~~ | **`docs/80_架构分析/01`–`04` 已随 `docs/` 删除** |
| ~~服务端~~ | **`fluentwork-backend/docs/104_架构分析/` 已随 `docs/` 删除** |
| 本仓怎么构建、门禁是什么、有什么已知漂移 | `fluentwork-ios/README.md` / `AGENTS.md`（2026-09-25 重建） |
| 服务端的对应条目 | 同目录 [`问题总清单-backend架构.md`](./问题总清单-backend架构.md) |
| 需要你拍板的四件事 | 本文件 §1 |
