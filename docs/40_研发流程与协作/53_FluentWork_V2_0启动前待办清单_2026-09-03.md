# FluentWork V2.0 启动前待办清单与执行顺序

**版本**：V1.1　**日期**：2026-09-04　**状态**：执行前门禁
**对应**：PRD V1.4 / 后端技术架构设计 V2.0 (`39_`) / 第二波收口评估 (`56_`) / 下半波规划 (`64_`) / 周末作战地图 (`54_`)
**定位**：盘点 V2.0 实施开工前**必须完成**的待办项，按"决策 → 文档 → 凭证 → 代码"四层顺序列出，给出执行顺序与责任建议
**变更记录**：
- V1.1 (2026-09-04 凌晨): 新增"五之二 线上紧急 follow-up"章节，把 GitHub backend #21 / #28 / #42 / #43 4 个 OPEN issue 纳入清账

---

## 一、门禁结论（先看这一条，更新：2026-09-04 凌晨）

> **V2.0 实施开工前有 3 项硬阻塞、9 项软阻塞**。硬阻塞未解决前不应启动 V2.0 代码层工作。
>
> **硬阻塞（仍未解除）**：(1) B8 LLM 真实接入；(2) 火山 Ark LLM/TTS 凭证；(3) 5 个开放技术决策拍板。
>
> **今夜进展（Layer 2 文档清账）**：
> - ✅ D-1~D-5 决策备忘录就绪，周六上午会议直接使用
> - ✅ B7 LLM 注入接口契约完成（解锁 B19）
> - ✅ A4 隐私声明 + F3 收藏置顶 + B4 重播规格完成（第二波 OPEN 清账）
> - ✅ 评估集 100 条样本设计完成（B8/B18 验证必需）
> - ⏸ Layer 2 剩余 DC-3（接口契约冻结）+ DC-4（数据库迁移脚本）
>
> **Layer 0 门禁仍 OPEN**：周六上午必须完成 D-1~D-5 拍板，方可解锁 Layer 3 代码层工作。
>
> **2026-09-04 凌晨新增（线上紧急 follow-up）**：
> - 🔴 **#43** Volc Duplex 空闲断连已发生真机事故（`provider_audio_failed` → `录音失败`），**W2 内必修**
> - 🔴 **#42** `cmd/voice-gateway` 主进程未挂 BadgeEmitter，真实链路徽章永远不亮，**W2 内必修**
> - 🟠 **#28** 评估集 / Prompt 回归基线缺失，B8 提测门禁悬空，**W3 前必建**
> - 🟠 **#21** review worker 仍为 stub + `ai_cost_logs` 零引用，B8 唯一阻断项延伸面，**W3 启动前必修**
>
> → 详见 **五之二：线上紧急 follow-up（2026-09-04 凌晨发现）**。

---

## 二、盘点方法论

待办项按以下四层归类：

```
Layer 0: 决策层（必须先拍板）
Layer 1: 凭证层（依赖供应商商务）
Layer 2: 文档层（接口契约、Issue 拆分）
Layer 3: 代码层（实际编码实现）
```

低层未完成，禁止启动高层工作。

---

## 三、Layer 0：决策层（5 项开放决策，必须先拍板）

> 来源：`39_FluentWork后端技术架构设计V2_0_2026-09-03.md` §十二

### D-1: 对话 LLM 模型选择

| 选项 | TTFT | 质量 | 成本 |
|---|---|---|---|
| **火山 Ark 深度思考模型** (`ep-20250620`) | 较慢（~1.2s） | 高（首字思考慢但回复深） | 高 |
| **火山 Ark Mini** | 快（~0.5s） | 中（适合短回复） | 低 |
| **混合策略**：首字用 Mini 兜底，超时后切深度思考 | 稳定 | 折中 | 折中 |

**建议**：混合策略——**默认用 Mini 满足 P90 ≤ 1.5s 红线**，用户特定场景（设计评审、方案讨论）触发深度思考。

**决策责任**：产品 + 技术共同拍板，**W3 启动前必须定**。

### D-2: TTS 供应商

| 选项 | 流式 TTFT | 音色丰富度 | 成本 | 集成成本 |
|---|---|---|---|---|
| **火山 TTS（同云）** | 低（~300ms） | 中等 | 低 | 零（已在火山生态） |
| **Azure TTS** | 中（~500ms） | 高 | 中 | 中（新增供应商） |
| **ElevenLabs** | 低（~300ms） | 极高 | 高 | 高（V2.0 考虑） |

**建议**：MVP 用火山 TTS（同云，零集成成本），**音色限制在每日一读场景通过 prompt 工程解决**。

**决策责任**：产品，**W3 启动前必须定**。

### D-3: 闪测 LLM 判定模型

| 选项 | 判定延迟 | 判定质量 | 备注 |
|---|---|---|---|
| **Ark Mini** | ~1.5s | 中（适合简单语义等价） | 成本低 |
| **Ark 标准模型** | ~2.5s | 高（复杂语义） | 延迟接近 SLA 边界 |

**建议**：Ark Mini + 精心构造的 Prompt（避免模型思考），判定延迟目标 ≤ 1.5s。

**决策责任**：技术，**W6 闪测开发前必须定**。

### D-4: 闪测间隔重复算法

| 选项 | 实现复杂度 | 调度效果 | 备注 |
|---|---|---|---|
| **简化版 SM-2**（V2.0 doc §5.3 设计） | 低（~100 行 Go） | 够用 | 当前设计 |
| **完整 SM-2 Anki 算法** | 中（~300 行 Go） | 更精准 | 需引入 interval/repetitions/lapses 字段 |
| **FSRS 4.0**（2023 最新） | 高（需建模） | 最优 | 学习成本高 |

**建议**：MVP 用**简化版 SM-2**，V1.1 视效果再升 FSRS。

**决策责任**：产品 + 技术，**W6 闪测开发前必须定**。

### D-5: 每日一读生成频率

| 选项 | 资源消耗 | 时效性 | 实现 |
|---|---|---|---|
| **凌晨 02:00 批处理**（V2.0 doc §6.1 设计） | 可预估 | 早 7 点前已就绪 | Cron + Redis Stream |
| **用户首次打开时按需生成** | 不可预估 | 实时 | 同步 5-10s + 前端轮询 |
| **混合：批处理兜底 + 按需补生成** | 折中 | 折中 | 更复杂 |

**建议**：**凌晨批处理**为主，**新用户首日按需生成兜底**（无前一日批次）。

**决策责任**：技术，**W6 内容模块前必须定**。

### Layer 0 决策完成状态（2026-09-03 夜场）

| 决策 | 文档位置 | 推荐方案 | 周六拍板状态 |
|---|---|---|---|
| **D-1** 对话 LLM 模型 | `47_D1_D5_开放技术决策备忘录.md` | Ark Mini（主）+ 深度思考（异步） | ⏸ 周六上午会议拍板 |
| **D-2** TTS 供应商 | `47_D1_D5_开放技术决策备忘录.md` | 火山独立 TTS（主）+ 双工内置（fallback） | ⏸ 周六上午会议拍板 |
| **D-3** 闪测判定模型 | `47_D1_D5_开放技术决策备忘录.md` | Ark Mini（成本敏感） | ⏸ 周六上午会议拍板 |
| **D-4** 调度算法 | `47_D1_D5_开放技术决策备忘录.md` | 简化 SM-2（V2.0 MVP） | ⏸ 周六上午会议拍板 |
| **D-5** 每日一读生成 | `47_D1_D5_开放技术决策备忘录.md` | 批处理 04:00 UTC + 按需兜底 | ⏸ 周六上午会议拍板 |

> **决策备忘录已就绪**：`docs/40_研发流程与协作/47_D1_D5_开放技术决策备忘录_2026-09-03.md`。周六上午 09:00 会议直接使用本文作为会议材料，拍板后填写上表"周六拍板状态"列。

---

## 四、Layer 1：凭证层（依赖供应商商务）

### C-1: 火山 Ark LLM 凭证

| 项 | 内容 | 状态 | 阻塞 |
|---|---|---|---|
| API Key | 火山 Ark 控制台开通 | ⏸ 待申请 | **B8 启动硬阻塞** |
| 模型 ID | 确认 `ep-20250620`（深度思考）/ `ep-mini` 可用 | ⏸ 待验证 | **B8 启动硬阻塞** |
| 并发配额 | QPS / TPM 上限与商务确认 | ⏸ 待谈判 | **压测前置** |
| 成本协议 | 单会话成本上限与商务确认 | ⏸ 待谈判 | **W8 压测前置** |

**SLA**：W3 启动前必须到位（即 9/10 之前）。

### C-2: 火山 TTS 凭证

| 项 | 内容 | 状态 | 阻塞 |
|---|---|---|---|
| 流式 TTS API Key | 控制台开通 | ⏸ 待申请 | **TTS 启动硬阻塞** |
| 音色列表 | 确认支持的美式/英式音色 ID | ⏸ 待选型 | **C5 启动阻塞** |
| 批量 TTS 配额 | 每日一读生成额度 | ⏸ 待确认 | **C5 启动阻塞** |

**SLA**：W3 启动前必须到位。

### C-3: TOS 对象存储

| 项 | 内容 | 状态 | 阻塞 |
|---|---|---|---|
| Bucket 创建 | `fluentwork-tts`, `fluentwork-recordings` | ⏸ 待创建 | C5 |
| 跨域策略 | 移动端预签名 URL 下载 | ⏸ 待配置 | C5 |
| 跨区域复制 | 灾备 | ⏸ 待配置 | W8 |

---

## 五、Layer 2：文档层（接口契约、Issue 拆分）

### DC-1: V2.0 架构文档评审通过

- [x] **已完成**：`39_FluentWork后端技术架构设计V2_0_2026-09-03.md` 已落地
- [ ] **待完成**：技术评审会议（至少 2 名后端 + 1 名前端 + 产品参与）
- [ ] **待完成**：评审意见修订

**SLA**：W2 末（即 9/14 之前）。

### DC-2: V2.0 Issue 拆分（跨仓）

> 参考 `52_FluentWork第二波建Issue与执行顺序清单.md` 模式

按 V2.0 §十一排期，建议先建 5 个 Epic + 18 个一级 Issue：

#### meta Epic（5 个）

| Epic | 内容 | 阻塞 |
|---|---|---|
| `EPIC: V2.0 LLM 编排与生成层` | AIOrchestrator + TTS + 闪测的统一编排 | D-1, D-2, D-3, D-4 |
| `EPIC: V2.0 真实 LLM 接入（B8 收口）` | 评价/炼化/每日一读内容生成 | D-1, C-1 |
| `EPIC: V2.0 即时反馈闭环（B7 完善）` | 命中检测 + 上下文注入 | D-1 |
| `EPIC: V2.0 素材输入模块（A1/A2）` | 创建练习弹层 + 提炼等待 loading | — |
| `EPIC: V2.0 闪测与话题建议（W7 范围）` | E1-E5 + H1-H4 | D-3, D-4 |

#### backend 一级 Issue（10 个）

| Issue | 标题 | 阻塞 |
|---|---|---|
| `B16: AIOrchestrator 骨架 + LLM 接口` | LLM 编排层 PoC，含 Prompt 模板 | D-1, C-1 |
| `B17: TTS Provider + 流式集成` | 火山 TTS Provider，含对话/批量两种模式 | D-2, C-2 |
| `B18: review eval 真实 LLM 接入（B8 收口）` | 评价三层 + 炼化三元组结构化输出 | D-1 |
| `B19: B7 命中信号注入 LLM 上下文` | 系统 Prompt 构造 + 命中信号注入 | D-1 |
| `B20: 每日一读内容生成 + TTS` | LLM 生成 + TTS 批量 + TOS 上传 | D-2, C-2, C-3 |
| `B21: 素材模块（A1/A2 后端）` | POST /materials + 异步提炼 + refine_json | — |
| `B22: 闪测模块（E1-E3 后端）` | /drill/round + /drill/judge + 调度更新 | D-3, D-4 |
| `B23: 话题卡生成（H1-H2 后端）` | 阈值判断 + LLM 生成 + 持久化 | D-1 |
| `B24: 历史回顾（C4）` | GET /sessions 列表 + 复用回顾接口 | — |
| `B25: 收藏置顶（F3 后端）` | PATCH /corpus/blocks/:id/favorite | — |

#### iOS 一级 Issue（8 个）

| Issue | 标题 | 阻塞 |
|---|---|---|
| `I14: 创建练习弹层（A1/A2 前端）` | 文本粘贴 + 一句话 + 提炼等待 loading | B21 |
| `I15: 说的房间 TTS 播放集成` | 收到 Opus 音频流即播 + 打断兼容 | B17 |
| `I16: 完整转录浮层（C1 增强）` | 完整话轮 + 录音回听 | B18 |
| `I17: 闪测 UI（E1/E4/E5）` | 训练卡流 + 结算页 + 推送 | B22 |
| `I18: 话题卡 UI（H1/H2/H3）` | 话题卡 + 聊后打卡 | B23 |
| `I19: 历史回顾列表（C4）` | 工作台历史入口 + 详情跳转 | B24 |
| `I20: iOS Prompt 工程师接入（V2.0 收口）` | 已部分完成，剩余 turn 超时兜底 | — |
| `I21: 状态机子状态扩展（V2.0）` | waitingForAIAnswer / waitingForEvaluation | — |

**SLA**：W2 末完成 Issue 拆分 + 进入 backlog，W3 开始建仓内 issue。

### DC-3: 接口契约文档冻结

V2.0 文档 §八只列了**增量**接口契约，需要补充完整 REST 接口契约冻结版：

- [ ] **待完成**：与现有 V1.2 §四合并整理为 `41_FluentWork_V2_接口契约冻结.md`
- [ ] **待完成**：WSS 帧协议 V2.0 冻结版（包含 ai.text.delta 等新帧）

**SLA**：W2 末。

### DC-4: 数据库迁移脚本

- [ ] **待完成**：drill_records 表 DDL（V2.0 §7.3 已定义，需写入 `migrations/`）
- [ ] **待完成**：utterances 表增量字段（hit_block_ids, llm_eval_json）
- [ ] **待完成**：phrase_blocks 调度索引复核（V1.2 已有，确认覆盖率）

**SLA**：W3 启动前入库。

### Layer 2 文档完成状态（2026-09-03 夜场）

| 文档 | 对应项 | 文档路径 | 状态 |
|---|---|---|---|
| B7 LLM 注入接口契约 | DC-2 扩展 | `docs/30_技术方案/44_B7_LLM注入完整接口契约_2026-09-03.md` | ✅ **今夜完成** |
| A4 隐私声明规格 | 第二波 OPEN | `docs/30_技术方案/45_A4_隐私声明与级联删除完整规格_2026-09-03.md` | ✅ **今夜完成** |
| F3 收藏置顶 + B4 重播规格 | 第二波 OPEN | `docs/30_技术方案/46_F3_B4_收藏置顶与重播完整规格_2026-09-03.md` | ✅ **今夜完成** |
| D-1~D-5 决策备忘录 | Layer 0 | `docs/40_研发流程与协作/47_D1_D5_开放技术决策备忘录_2026-09-03.md` | ✅ **今夜完成** |
| 评估集 100 条样本设计 | B8 验证 | `docs/50_测试与验收/57_FluentWork评估集100条样本设计_2026-09-03.md` | ✅ **今夜完成** |
| 周末作战地图 + Issue 草案 | DC-1 扩展 | `docs/40_研发流程与协作/54_FluentWork_V2_0周末作战地图与Issue草案集_2026-09-03.md` | ✅ **已提交 a3f498a** |
| DC-3 接口契约冻结 | `41_` | — | ⏸ W2 末 |
| DC-4 数据库迁移脚本 | `migrations/` | — | ⏸ W3 前 |

> **Layer 2 门禁已降低**：今夜完成 5/6 项文档。剩余仅 DC-3 接口契约冻结和 DC-4 数据库迁移脚本需 W2-W3 完成。

---

## 五之二：线上紧急 follow-up（2026-09-04 凌晨发现）

> 来源：2026-09-03 夜场真机回归 + GitHub `FluentWork/fluentwork-backend` 仓库 OPEN issue 盘点（4 条）。
> 定位：V2.0 启动前**已存在但未纳入本文**的待办项；按"线上事故 → 阻断门禁 → 顺带清理"三档排序，给出 Owner / 工期 / SLA / 排期。

### F-1: 4 条 OPEN Issue 概览

| Issue | 标题 | 档位 | 状态摘要 |
|---|---|---|---|
| **[#43](https://github.com/FluentWork/fluentwork-backend/issues/43)** | B15-followup: keep alive / auto-reopen idle Volc duplex audio path | 🔴 P0 线上事故 | 2 min 空闲后恢复说话 → `broken pipe` → 录音失败（真机 09-03 捕获）；iOS 友好文案已落地（`d155382`），后端 keepalive/reopen 缺失 |
| **[#42](https://github.com/FluentWork/fluentwork-backend/issues/42)** | B12-followup: wire corpus-backed BadgeEmitter for live voice-gateway | 🔴 P0 线上事故 | `cmd/voice-gateway` 主进程从未 `SetBadgeEmitter(...)`，真实火山链路 `feedback.badge` 永远不亮；turn_id 与 iOS 未对齐 |
| **[#28](https://github.com/FluentWork/fluentwork-backend/issues/28)** | B15: build offline evaluation dataset and prompt regression baseline | 🟠 P1 阻断门禁 | 评估集 0 条，Prompt 回归基线缺失，B8 提测门禁悬空；评估集设计 09-03 已完成（见 `57_`），**差数据收集 + 回归入口** |
| **[#21](https://github.com/FluentWork/fluentwork-backend/issues/21)** | B8: implement review evaluation and refine in ai-worker | 🟠 P1 阻断门禁 | review worker 仍为 stub，评价/炼化未真实接入；`ai_cost_logs` 表已建（migration 0002）但代码库零引用 |

### F-2: 修复排期 + Owner

> **责任约定**：
> - **Owner** = 该 issue 实施负责人（**待周六站会拍板**，下表给出推荐人选）
> - **Reviewer** = 合入主线前的二审（默认架构师 / 后端 Tech Lead）
> - **SLA** = 最晚合入日期；逾期未完成则 V2.0 W3 启动延期

| Issue | 档位 | 工期 | 推荐 Owner | Reviewer | 依赖 | SLA | 排期落点 |
|---|---|---|---|---|---|---|---|
| #43 | 🔴 P0 | 2d | 后端语音组（**@voice-gateway 当周值班**） | 后端 TL | 无（独立可发） | **9/8 (周二)** | W2 提前，与 D-1~D-5 决策会并行 |
| #42 | 🔴 P0 | 2d | 后端语音组（**@voice-gateway 当周值班**） | 后端 TL | MySQL DSN 配齐（已有） | **9/8 (周二)** | W2 提前，与 D-1~D-5 决策会并行 |
| #28 | 🟠 P1 | 4d | 后端 AI 组 + 测试组 | 后端 TL | 评估集设计（已就绪 57_） | **9/13 (W2 末)** | W2 后半段 |
| #21 | 🟠 P1 | 5d | 后端 AI 组 | 后端 TL | D-1 决策、#28 评估集 | **9/20 (W3 末)** | W3 启动前 |

> **关键约束**：
> - **#43 与 #42 同 Owner 串行**（都碰 voice-gateway 共享代码区），合并 4d 工期 = W2 周一至周三。
> - **#28 是 #21 的前置**：B8 真实接入前必须有可跑回归基线，否则发车即裸奔。
> - **#21 是 V2.0 唯一阻断项（B8）的延伸**：原计划 B8 在 W5 收口，但 #21 的 stub 状态意味着 review worker 现在还没接到真实 LLM；如果不提前，B18 启动时反而要回头补。

### F-3: 验收口径（每条 Issue 收口时自查）

#### #43 验收
- [ ] idle ≥ 3 min 后恢复说话，对话继续不报错（真机 1 次 + 脚本化 5 次）
- [ ] 若无法 reopen：仅发 1 次稳定 `provider_audio_failed`，iOS 干净重试不重发
- [ ] `go test ./internal/voicegateway/... -count=1 -race` 全绿
- [ ] 新增 `keepalive_ping.go` 单测覆盖：连续 3 min 无语音 + 突然 audio forward 双向通

#### #42 验收
- [ ] 真机 Volc Duplex 链路出现 `feedback.badge emitted`，`phrase_block_id` / `turn_id` 均非空
- [ ] iOS `state.badgeFeedback.entries[].turnID` 与后端完全一致（53 号验收清单 §2.4 四现象可跑）
- [ ] 无命中场景**不发**帧；badge 800ms 超时/失败不影响音频流 / 转录 / `session.end`
- [ ] `go test ./internal/voicegateway/... -count=1` + `-race` 定向测试绿
- [ ] 新增 handler 级集成测试覆盖：live provider 命中 → 帧；未命中 → 不帧；DB 不可用 → 不影响主链路

#### #28 验收
- [ ] 评估集样本 ≥ 50 条（来源 / 标注口径有记录）
- [ ] 回归入口**一条命令可跑**，输出通过/失败 + 失败明细
- [ ] **B8 / B18 提测门禁**：评估集全绿才允许提测
- [ ] 评估集本身**不进入生产链路**，不暴露用户数据（内测前用合成/演示素材）

#### #21 验收
- [ ] 评价 + 炼化合并为单次旗舰模型调用，输出 JSON Schema 校验
- [ ] 失败重试 1 次，重试仍失败写 `review_jobs.error` 死信
- [ ] 幂等键 `session_id + task_type`，重复消费直接跳过
- [ ] **成本落账必同步**：`ai_cost_logs` 与 review 结果同一事务，`task_type` / `model` / `tokens_in` / `tokens_out` / `cost_fen` 字段全填
- [ ] 完成后 `session.status = reviewed` 由本路径驱动
- [ ] Prompt 以 `prompt_configs` 最小配置 + 版本字段，避免硬编码

### F-4: 风险与回退

| 风险 | 触发条件 | 回退 |
|---|---|---|
| #43 keepalive 改动引发 Volc 上游限流 | Volc 商务侧 QPS 上限 | 改为"被动重连"（空闲后下一次 audio forward 才触发），零额外 QPS |
| #42 emitter 接入后命中激增导致 iOS 抖动 | 语料库量大于预期 | 加 `badge` 限流：每 session ≤ N 条/分钟（具体值在 W2 内与 iOS 联调定） |
| #28 评估集数据准备延期 | 标注人手不足 | 退而求其次：先用 30 条合成样本跑通回归入口，达标线后续补 |
| #21 stub 替换过程中现有 #13/#14 真机回归挂掉 | B12 仍依赖部分 review worker stub 接口 | 灰度：先发 feature flag，shadow 模式跑 3 天再切 |

### F-5: 跟踪与同步

1. **每日站会**：Owner 报这 4 条进度，逾期即升级
2. **周末看板**：`docs/40_研发流程与协作/64_` 周报新增"线上紧急 follow-up"小节
3. **GitHub 同步**：
   - #43 / #42 加 `priority: P0` + `v2.0-blocker` 标签
   - #28 / #21 加 `v2.0-blocker` 标签
   - 每条 issue 的"实现计划"区块 link 回本文 §F-3
4. **本节状态机**：
   - 🔴 P0（W2 必须清完）→ ⏳ 实施中 → ✅ Closed & 验证
   - 🟠 P1（W2-W3 必清完）→ ⏳ 实施中 → ✅ Closed & 验证
   - 全清后**本节归档**到"七 第二波遗留项"末尾作为历史记录

---

## 六、Layer 3：代码层（实际编码，按 Epic 分组）

> 已按 V2.0 doc §十一 排期顺序排列

### EPIC-A: V2.0 LLM 编排与生成层（W3-W5 主线）

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B16 | 5d | D-1, C-1 | /orchestrator/chat 流式接口通，首字 ≤ 800ms |
| B17 | 3d | D-2, C-2 | 对话 TTS 流式 + 每日一读批量均通 |
| I15 | 2d | B17 | iOS 收到 ai.text.delta 帧即播 TTS 音频 |

### EPIC-B: V2.0 真实 LLM 接入（B8 收口，**唯一阻断项**）

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B18 | 5d | B16 | review 接口返回真实三层评价 + 炼化三元组；P90 ≤ 15s |
| I16 | 2d | B18 | 回顾页 UI 切真实数据，stub 数据移除 |

**SLA**：W5 末必须完成，是 C5/E/H 全部下游功能的唯一前置。

### EPIC-C: V2.0 即时反馈闭环（B7 完善）

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B19 | 2d | B16 | 命中信号注入 LLM 上下文；下轮 AI 回复自然引用 |
| I11 | 1d | B19 | 徽章 UI 验证（已实现，需联调） |

### EPIC-D: V2.0 素材输入模块

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B21 | 3d | — | POST /materials + 异步提炼 + refine_json |
| I14 | 2d | B21 | 创建练习弹层 + 提炼等待 loading（PRD A1 强制要求） |

### EPIC-E: V2.0 闪测与话题建议（W7 范围）

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B22 | 4d | D-3, D-4 | /drill/round + /drill/judge；语义判定 P90 ≤ 2s |
| B23 | 3d | D-1 | 阈值判断（≥20 块）+ LLM 生成话题卡 |
| I17 | 3d | B22 | 闪测 UI 完整闭环 |
| I18 | 2d | B23 | 话题卡 UI + 打卡 |

### EPIC-F: V2.0 杂项补完

| Issue | 工期 | 依赖 | 验收 |
|---|---|---|---|
| B20 | 2d | B17 | 每日一读内容生成 + TTS |
| B24 | 1d | — | 历史回顾接口 |
| B25 | 1d | — | 收藏置顶后端 |
| I19 | 1d | B24 | 历史回顾列表 UI |
| I21 | 2d | — | 状态机子状态扩展 |
| I20 | 2d | — | turn 超时兜底（已有部分） |

---

## 七、第二波遗留项（W3 前可补完）

> 来源：`56_FluentWork第二波PRD对照检查与收口评估_2026-09-03.md` §五.2

| 项 | 影响 | 建议 |
|---|---|---|
| A4 隐私声明 | 提审必备 | W3 前补完（iOS 设置页 + Backend 隐私删除） |
| B4 AI 气泡重播 | UX 体验 | W4-B 工作中合并到 I15 |
| F3 置顶 | 语料库功能不完整 | W6 合并到 B25 |
| C4 历史回顾 | 工作台体验 | W6 合并到 B24 |

---

## 八、执行顺序（建议）

```
W2 (现在 - 9/13):
  ✅ 完成 V2.0 启动前待办清单文档（本文）
  □ 拍板 Layer 0 五个开放决策（D-1 ~ D-5）
  □ 申请火山 Ark LLM + TTS 凭证（C-1, C-2）
  □ 完成 V2.0 架构文档评审（DC-1）
  □ 拆分 V2.0 Issue（DC-2）
  □ 数据库迁移脚本入库（DC-4）
  🔴 #43 Volc Duplex 空闲断连修复（2d, 周一-周二）
  🔴 #42 voice-gateway BadgeEmitter 接线（2d, 周一-周二）
  🟠 #28 评估集 + 回归基线建设（4d, 周三-周六）

W3 (9/14 - 9/20):
  □ B16 AIOrchestrator 骨架
  □ B17 TTS Provider
  □ B21 素材模块后端
  □ I14 创建练习弹层
  □ A4 隐私声明补完
  🟠 #21 B8 review worker 真实接入（5d, 周一-周五）— V2.0 唯一阻断项前置

W4 (9/21 - 9/27):
  □ B16 完成 + LLM 流式联通
  □ B17 完成 + TTS 流式联通
  □ I15 说的房间 TTS 集成
  □ I14 创建练习弹层完成
  □ B4 合并到 I15

W5 (9/28 - 10/4):
  ✅ B18 review 真实 LLM（B8 收口 — 唯一阻断项）
  □ B19 B7 上下文注入
  □ I16 完整转录浮层
  □ B20 每日一读内容生成

W6 (10/5 - 10/11):
  □ B22 闪测模块
  □ B23 话题卡生成
  □ B24 历史回顾
  □ B25 收藏置顶
  □ F3 合并到 B25

W7 (10/12 - 10/18):
  □ I17 闪测 UI
  □ I18 话题卡 UI
  □ I19 历史回顾 UI
  □ I21 状态机扩展
  □ I20 turn 超时兜底
  □ 压测 + 提审

W8 (10/19 - 10/25):
  □ 审核修复
  □ 新手引导
  □ 隐私功能打磨
  □ 20 人内测启动
```

---

## 九、跟踪与更新规则

1. **每周一更新**：本文档作为 V2.0 周看板，每周一更新状态
2. **决策变更记录**：Layer 0 五项决策变更必须修订本文档 + 同步给全栈
3. **凭证状态追踪**：Layer 1 凭证开通状态实时更新，blocked 项每日站会必问
4. **Issue 关联**：每条 Issue 必须 link 回本文档对应章节

---

## 十、对接关系

| 本文档 | 上游 | 下游 |
|---|---|---|
| 启动前门禁层 | `39_V2.0 架构设计`、`56_第二波收口`、`64_下半波规划` | `40-41_执行清单`、`50-51_验收清单` |

---

## 十一、当前快照

> **本文档生成于 2026-09-03 第二波收口日**，反映 V2.0 实施启动前的真实待办状态。
> **2026-09-04 凌晨 V1.1 更新**：新增 4 条 backend OPEN issue 清账（详见 五之二）。
>
> - 5 个开放决策待拍板（D-1 ~ D-5）
> - 2 个凭证待申请（C-1, C-2）
> - 18 个 V2.0 一级 Issue 待建（DC-2）
> - 2 个 🔴 P0 线上 follow-up 待清（#43 / #42）
> - 2 个 🟠 P1 阻断门禁待清（#28 / #21）
> - B8 为唯一阻断项，必须 W5 完成
>
> **建议用户在 W2 内（9/13 之前）完成 Layer 0 + Layer 1 的全部决策与凭证**，
> **并在 W2 内（9/8 之前）清完 2 个 P0 线上 follow-up**，否则 V2.0 W3 启动将延后。

---

*文档版本：V1.0*
*上游依据：`39_V2.0架构`、`56_第二波收口`、`64_下半波规划`、`PRD V1.4`*
*下游交付：V2.0 一级 Issue（待建）、第三波验收清单（待建）*
