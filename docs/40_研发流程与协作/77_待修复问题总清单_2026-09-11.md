# FluentWork 待修复问题总清单

**版本**：V2.3  
**日期**：2026-09-11  
**本次新增**：**P1-3 / P1-9 / P1-11 / P1-12 / P1-13 / P1-15 / P1-19 / P1-23 八条闭合**。  
- **P1-12 / P1-13** 是 WSS 扫描里"小改动、无联动"那组的①②（③ **P1-14 经核实不是小改动**：需动 schema + 会话记录 + 网关标记）。P1-13 修的是同一个标记上的**三**处，台账只记了一处 —— 第三处是"先写测试"直接兑现的收益。  
- **P1-3** 相位收敛 13 → 9，测试一条不少。红验证抓出**两条空洞守卫**，都是本次自己刚写的。  
- **P1-15** 两条死路：**标记 + 钉住，未删除**。核实后两条**都补不上** —— ①的 `review` 信号只能来自网关（网关不发），②重连 iOS 单边接不了。**运行时行为零改动。**  
- **P1-9 / P1-11 / P1-19（本轮新增）**：三条都**比台账的描述更彻底** —— P1-9 不是"单例缓存破坏隔离"而是**作用域根本不在容器里**（改全部 25 处）；P1-11 是代码**与自己的立场不一致**而不只是"分类过宽"；P1-19 的根因是"**少一条不是错误**"，兜底分支总能产出字符串。三条各挖出一个副产品：P1-9 撞出 Swift 6.3 编译器崩溃，P1-11 抓到 provider 层实际日志，P1-19 发现 `unsupported_frame` 已废弃。  
- **P1-23**：设计文档称"上下文在 Redis"，**网关侧零 Redis 依赖** —— 正是 P1-15 后半"接不了"的直接原因。已在 `32_` 就地划掉原文并附"已知偏差"表（保留划掉的原文，不删 —— 它是"设计写了、实现没做、文档还引用着"的样本）。  
**定位**：**唯一入口**。把架构、后端、前端、以及 PRD 评审四路来的问题汇成一张可逐条执行的表。逐条修、逐条勾。  
**配套**：[`78_会话交接与下一步`](./78_会话交接与下一步_2026-09-11.md)（**下次会话从这里读起**）· [`54_传输层决策`](../30_技术方案/54_传输层不引入第三方WebSocket框架_2026-09-11.md) · [`52_架构图`](../30_技术方案/52_语音链路架构图_2026-09-10.md) · [`53_架构评价`](../30_技术方案/53_语音链路架构评价_2026-09-10.md) · [`74_实现方式评估`](../70_外部研究/74_语音实现方式再评估_2026-09-10.md)  
**纪律**：每条缺陷按 `agents/shared/defect-fix-discipline.md` —— **先复现测试,再修,留下守卫,并破坏实现证明守卫咬得住**（红的必须是预料的那条;红的是别的或全绿,说明测错了东西）。

---

## 0. 前提:方案已定

**继续自研 relay,不引入官方客户端语音 SDK。**

**同时:传输层不引入第三方 WebSocket 框架**（继续用 `URLSessionWebSocketTask`）。依据、义务与推翻条件见 [`54_传输层不引入第三方WebSocket框架`](../30_技术方案/54_传输层不引入第三方WebSocket框架_2026-09-11.md)。

这不是"因为厂商没有 SDK"—— `74_` 已核实:火山的全双工 iOS SDK 存在,而且指向的就是我们在用的那个端点。是**选择**自研,不是**被迫**。

理由(按重要性)：客户端零凭证是硬约束；转录/事件/置信度必须留在服务端一手可见；官方 SDK 的价值集中在 **AEC + 音频管线**,而**可靠性它也不给**(其文档无重连/恢复/重试章节)。

**分层立场**(`74_` §4.1)：服务端调 ASR/LLM/TTS 用裸协议；iOS 播放音频可以用现成解码方案；采集自研；**AEC 必须明确来源**。

---

## 1. 已修复(2026-09-10 一夜,供追溯,不必再动)

| # | 问题 | 仓 | 证据 |
|---|---|---|---|
| F1 | 录音轮次外 PCM 上行 → transcript 串轮 | ios | `docs/40` + `4adc59a` |
| F2 | 说房间交互冗余(2 次点击 / 「结束本轮」实为结束会话) | ios | `docs/41` + `8c1c22d` |
| F3 | `tapToStart` 误用自动 VAD 的 1.5s 静音阈值 → 句子被截断 | ios | `dc5d520` |
| F4 | 异常退出丢整场 session(含全部 utterances) | backend | `docs/42` + `c084a2c` |
| F5 | `partial` 掩盖读错误(把"连接已死"报成"供应商慢") | backend | `docs/43` + `c084a2c` |
| F6 | volc-duplex 不发开场帧 → iOS 卡 `aiSpeaking`,首轮误发 `interrupt` | backend | `docs/44` + `c084a2c` |
| F7 | 读失败不重置 duplex + reopen 预算整场只有一次 → **三轮必挂** | backend | `docs/45` + `dbe3df1` |
| F8 | `dev-up.sh` 迁移不幂等 → 重启起不来 | backend | `docs/46` + `38152da` |
| F9 | **Volc 音频帧超 32 KiB 默认读上限 → 每轮断连**(「每轮失忆」根因) | backend | `docs/47` + `ea840e0` |
| F10 | 客户端单帧超 32 KiB → 踢连接 | backend | `docs/48` + `3e1efed` |
| F11 | **F3 的修复从未生效**：`startCapture()` 用默认参数重建 tracker，把刚配好的模式换回 `autoStart=true` + 1500ms → 真机一直是 1.5s，且能量能自己起一轮 | ios | `docs/44` + `f361650` |
| F12 | `playerNode.play()` 在停掉的引擎上**不抛错而是抛 NSException** → **app 终止**。触发于 `endSession`→`stopCapture()` 与在途音频帧的竞态 | ios | `docs/44` + `f361650` |
| F13 | **助手音频从来没被播放过**：`playerNode.play()` 全局零调用（音频排进一个从未启动的节点）；叠加传输层 `.bufferingNewest(64)` 丢帧（实测 200 帧进 / 64 帧出） | ios | `docs/43` + `63d233c` |
| F14 | **F12 的修复不够**：`engine.isRunning` 不是「能播」的判据 —— `engine.start()` 会成功（重建最小图即可）但节点仍挂在被拆掉的旧图上。异常原文的 "disconnected" 描述的是**节点**状态，不是引擎状态。改为守住自己的生命周期事件（`stopCapture()` 退役播放方向 + detach 节点 + 校验 `playerNode.engine`） | ios | `docs/44` + `88d0160` |
| F15 | **F12/F14 的修复都不够**：`play()` 的 `NSException` 不是状态问题，是**时机**问题 —— `attach`/`connect`/`play` 只在会话第一帧挤在同一个同步块里发生，而那一刻引擎已在运行，往运行中的 render graph 里改图后立刻 `play()` 会被判为"未连上"。改为 `startCapture()` 内、`engine.start()` **之前**建好整张图 | ios | `docs/44` + `10a0506` |
| F16 | `play()` 的 `NSException` 加 ObjC 兜底（`FWTryCatch`，新增 `FluentWorkObjCSupport` target）—— 前三轮都在预测它何时抛，而 Swift 捕不到 `NSException`。现在"节点无处可播"是一次丢帧 + 一条日志，不再是 app 终止 | ios | `docs/44` + `10a0506` |
| F17 | **不可恢复的系统中断 = 静默悬挂**：`.began` 挂起并停播，挂起期间状态机丢弃除 5 个事件外的全部输入；而 `.ended(shouldResume: false)` 什么都不发 —— 唯一能解锁的 `.systemInterruptEnded` 恰好是那条分支拒绝发出的。播放停止 + UI 冻结 + 后续语音事件全部石沉大海 | ios | `docs/45` + `55f969e` |
| F18 | **透明重开重置音频帧序号 → 整场永久静音**：`nextAudioSeq` 的注释写明「单调，因为客户端丢弃 ≤ barge-in 水位线的帧」，但 reopen-once 用 `Open()` 新建 provider session，序号从 1 重来、而客户端水位线还停在几百 → 之后每帧被**静默**丢弃。文字走另一条分支所以照常返回，会话不报错。**两仓之间的隐式契约被重开这条路径踩了** | backend | `docs/50` + `5d67561` |
| F19 | **评估超时剪掉话尾**：`(.waitingForEvaluation, .evaluationTimedOut)` 附带 `.stopPlayback`，注释自陈「leftover TTS is dropped」—— 但评估超时是固定 **20s** 预算，音频长度由 AI 说了多长决定（网关轮末一次性爆发）。真机实测 **276 帧 / 27.6 秒音频在 88ms 内推完，播放于 20.6s 被 `playerNode.stop()` 掐掉**。兄弟分支 `evaluationReceived` 落同一相位却**不停播** | ios | `docs/47` + `904316f` |
| F20 | **`.connecting` 是唯一没有超时的相位**：定时器覆盖了处理子阶段/评估/录音/重连/整轮上限，唯独漏了每个会话的第一个相位。任何一步没送到 `.socketReady` → 房间永远停在「连接中」，无超时、无错误。**症状已消除（看门狗 + 可重试错误），触发条件未定性** | ios | `docs/48` + `2341738`。**排查进展**：真机日志显示后端 **237ms** 就绪（`voice.duplex.open.done` 24.811），客户端却等到看门狗（10.58s）；且这 10 秒内客户端**一条 `timing_socket_receive` 都没有**—— 事件发出去了（`send(.sessionStart)` 成功证明 `.connected` 的 emit 已执行），消费者没反应。已加消费者生命周期埋点（`transport_consumer_*`），下一次卡住会直接指出是「没起来」还是「事件被丢」 | `docs/48` + `2341738` · `f2f7b7a` |
| F21 | **读连接的人被当成了会话资源**：传输层 `events` 是进程级 `AsyncStream`（`.singleton`），消费者却由 `.createSession` 建、`.endSession` 取消 —— 单消费者序列上多个短命迭代器互相抢元素，且会话结束的取消会落到**下一个**会话的消费者上，循环静默退出。真机表现就是 F20。**已确定性复现**（开始→结束→重进，第二个会话到不了 `.aiSpeaking`） | ios | `docs/49` + `d369997` · `0f3ad27` |

**顺带达成**:v2 turn 合同**已真机**。2026-09-10 22:36 会话留下了 `turn_id=turn-1` + `log_id=20260910220603...` —— `51_技术方案/51` 里那个"零样本"缺口已闭合。

---

## 2. 待修复

优先级判据:**能不能让产品闭环成立** > 结构合理性 > 体验打磨。

### P0 — 产品闭环缺件

| # | 问题 | 影响 | 下一步 |
|---|---|---|---|
| ~~**P0-1**~~ ✅ | ~~AI 在说的房间里没有声音~~ | **真机闭合**。根因链：网关侧转发已就绪（backend `ce32176`）但客户端 `playerNode.play()` 全局零调用（F13）→ 补上后连续三轮崩溃（F12/F14/F15）→ 在 `startCapture()` 内、`engine.start()` 之前建好整张图 + ObjC 兜底（F16）。**真机确认听到 AI 说话且不再崩溃**。教训："日志里有帧"不是"用户听见了" | —— |
| **P0-2** | **AEC 从未被验证** | 声音路径已打通（P0-7/P0-8），AEC **第一次真正可测**。裸 WebSocket 不提供 AEC | **下一步**：§3.3 的受控实验（AI 说独特短语 + 用户全程不出声） |
| ~~**P0-3**~~ ✅ | ~~语音链路零成本记账~~ | **已做**（backend `508e43f`/`ffaf018`/`3b74294` + `docs/51`）：provider 测量两个方向的音频 → `voice_usage` 随 `sessions/end` 上线 → app-server 在同一事务里写一条 `voice.duplex` 行。**`CostFen` 恒为 0** —— 用量是事实，费率待账单（P2-2）。**已知局限**：账本只有一列 `audio_sec`，上下行拆分被求和后丢失，见 `docs/51` §5 | —— |
| ~~**P0-4**~~ ✅ | ~~`input_audio_mute.commit` 未发~~ | **已补**（`762d26e` + `docs/53`）：轮末声明静音、下一轮第一块音频到达时解除。正好补上 `sendSilence` 覆盖不到的窗口 —— 它只在 `collectTurn` 期间跑，**两轮之间是空的**。**未真机验证**：验收条件是**两轮间隔 > 60s** 仍正常 | —— |
| ~~**P0-5**~~ ✅ | ~~卡壳救援缺失(PRD 层)~~ | **已进 PRD V1.6**：设计原则第 4 条此前是全文**唯一一条没有任何功能 ID 实现**的原则，现已补上 **B8 卡壳救援（P0）**——§5.4 完整机制（三层递梯子、不追问、不离开语音流、不记入失败）+ 功能详表 + W5 排期 + 埋点（首次卡壳救援触发率）。**并为 B8 自己记了一条风险**（「卡壳救援变成依赖」——梯子给太早会与主动召回目标相反），见风险表。**⚠️ 属 MVP 范围增补，按启动书 §四 需 CTO + PM 会签后生效** | —— |

### P1 — 结构与可观测

| # | 问题 | 影响 |
|---|---|---|
| ~~**P1-1**~~ ✅ | ~~B17 TTS 只有入口、零调用方~~ | **决策：标记未启用，不接线**（backend `79ded15` + `docs/54`）。消费者与生产授权**两样都没有**：每日一读的 `AudioURL` 只有读写、没有生成者；闪测只是导航里的一个 tab；prod SKU 仍 403（P2-4）。今天接线只会得到**在 dev 通过、在 prod 失败**的功能——把环境问题伪装成代码问题。**风险是文档风险不是代码风险**：代码正确，坏在它的存在暗示了一项能力。所以修法是让状态可读——包注释开门见山 `built, not turned on`。**红验证抓出一个空洞测试**：`Collect` 自己也挡 nil 并给出逐字相同的 503，第一条测试分辨不出守卫在不在；真正能区分的是**顺序**（无守卫时畸形 body 先得 400），据此补的第二条去守卫即红。**P2-4 是外部阻塞，保持开启** | —— |
| ~~**P1-2**~~ ✅ | ~~不是真流式~~ | **两半都已修**。**文本**（`e3fc02a` + `docs/55`）：`ai.text.delta` 已是真增量；iOS 侧一行未改——它的 reducer 本来就是 `last.text += delta`，只是生产者从没发过增量。**顺带修掉一条潜伏的契约违背**（AI 项会永远停在 `.streaming`，当前无视图读取所以不可见）。**音频**（`57e2128` + `docs/56`）：**这才是主导延迟**——整轮音频此前攒完才发，用户要等全部 TTS 生成完才听到第一个音节。现在逐块重采样并即时发出。**关键推导**：上游那个 `hi >= inCount` 钳位**从不触发**（对 3k/3k+1/3k+2 三种余数分别验算），所以输出不依赖总长度——流式可逐字节等价，且**不需要 flush**。**红验证的重点发现**：「流式 == 整段」那条测试**抓不住天真实现**（它永远只喂一块），只有**分块不变性**抓得住——等价性测试对"边界"这个唯一失效维度是盲的。**未真机验证**；且因 P1-5 未消费 `server_ts_ms`，**拿不到首响的量化改善**，只能靠听感 | —— |
| ~~**P1-3**~~ ✅ | ~~13 个相位超出产品语义~~ | **设计已提交**（ios `a50fea9` + `ios docs/50`）。**未实现，是刻意的**：改动面 ~62 处引用跨状态机/中间件/视图 + 5 个测试文件，而动的是"4 个真机缺陷里 3 个出在其中"的那个状态机 —— 要么一次做完要么不做，留半个收敛后的状态机比现状更难推理（"相位"会同时有两种含义）。设计要点：五个"相位"其实是一条后端管线（`processingASR→LLM→Review→waitingForEvaluation`），而 `processingSubStage` **已经存在**、只是从相位派生 —— 一份信息存了两遍。目标是让子阶段成为存储字段、相位回归产品语义，**13 → 9**。分两次提交：提交一机械重命名（13→11），提交二语义合并两个等待（11→9），一旦行为回归要能看出是哪一半。**三处缺陷与相位的真实关系**：F19 把 `.stopPlayback` 挂在 `(.waitingForEvaluation, .evaluationTimedOut)` 上（后端定时器变成用户可见行为）；F20 定时器按相位配置，少写一个条目那个相位就永不出来；F17 挂起逻辑要枚举相位。**已实现，13 → 9**（ios `9cd6fd8` 提交一 + `6ece0f0` 提交二 + `ios docs/54`），**测试 456 条一条不少，F19 的守卫测试未改一字**。**设计的前置条件答案是"三个里只有一个存在"**：后端全仓只产出 `orchestration`/`asr`/`tts`/`scheduler`/`transport`，**`llm` 与 `review` 没有生产者** —— 即 P1-20 的"只兑现 3/13"。它不改变做法（iOS 侧三个串保留原样，本来就非后端回显），**无后端改动**；但"先确认"的产出是一个否定答案，已入档免得下一个读设计的人以为那里有契约。**设计没覆盖的一个决定**：ASR → LLM 不再是相位变化，`trackTransition` 不会再发出 —— 日志会停在"进入 processing"，而"静默停止运行的步骤"与"从未运行过的步骤"完全同形（P1-15 的形状）。故 `trackTransition` 多带 `stage`，阶段推进时 `from == to == .processing`。**定时器交接从相位改到 stage** —— 本票唯一改行为处，且必须改：按相位写的条件在新世界里永远不成立，ASR 预算会数进 LLM 阶段而 LLM 预算永不装上（静默）。**`discardsTurnOnReconnect` 从相位属性变成状态属性 + 按 stage 分** —— 合并的唯一语义代价：`evaluation` 丢弃该轮、`aiAnswer`（I21 中止落点）不丢弃，红验证显示弄错会让落点降级并停播，即**掐掉用户欠着的那句回答**。**红验证抓出两条空洞守卫（都是本次自己写的）**：①不变量守卫起初**够不到自己要守的分支**，全绿；②定时器交接**没有任何测试覆盖**，456 条全绿 —— 已各补一条。**`processingReview` 已核实为生产死面**（唯一入口只有测试派发，后端也不发 `review`）。 |
| ~~**P1-4**~~ ✅ | ~~ASR 的 WER 从未测过~~ | **已测**（`fefcf9c` + `docs/52`）：真人样本 30 条，**总体 WER 14.7%（S=30 D=0 I=9 N=266）**，归一化掉数字/缩写格式差异后 **10.4%**。**`D=0` —— 一次「没听见」都没有**；失败全是**替换**（blocked→broke、pair→peel、cache→catch）。**结论：不迁移到分解流水线** | —— |
| ~~**P1-5**~~ ✅ | ~~`server_ts_ms` iOS 未消费~~ | **已消费**（ios `0309013` + `ios docs/51`）。关键发现：字段单独没有用 —— 两台机器的时钟没有关系，直接相减量到的是时钟差混合着延迟。真正的入口在后端：**网关只在 ping 的 `ts` 为 0 时才换成自己的时钟**，而客户端一直发自己的墙上时钟，服务端时钟从不过线。走 `ts: 0` 复用后端已有分支，**零后端改动**。另附一条：**首响其实不需要 offset** —— 两个端点都在客户端钟上；offset 买到的是把总延迟拆成上行/服务端/下行。447 tests passed，四次红验证。**未真机验证** | —— |
| **P1-6** | `abort` 后 ASR 是否串味未验 | 火山无 `clear` API,网关改为重建 duplex;待真机确认 |
| ~~**P0-7**~~ ✅ | ~~助手音频从未被播放~~ | **已修**（ios `63d233c`）：`playerNode.play()` 从未被调用 —— 音频排进一个从未启动的节点。这条路径是为测试写的（注释自陈"audio graph is irrelevant for the assertions we make"），所以一直没暴露 | —— |
| ~~**P0-8**~~ ✅ | ~~传输层丢帧~~ | **已修**（ios `63d233c`）：`.bufferingNewest(64)` 在轮末 106 帧爆发时丢最旧的，实测 **200 帧只收到 64**。同一条流还承载 `ai.turn.end`/`error`/`badge` —— 丢控制帧会卡死状态机。改为 `.unbounded` | —— |
| ~~**P0-6**~~ ✅ | ~~子阶段超时杀会话~~ | **已修**（ios `46caec9`）：iOS 子阶段预算 15/45/30s 却 dispatch `.failed`，而网关等 60s —— 客户端让服务端预算变得用不上。现降级为诊断，终局只由 `ai.turn.end` 或 70s 总上限决定 | —— |
| **P1-7** | **播放门禁水位线的生命周期错配（客户端有两道，都是整场）** | 水位线是**整场**生命周期，而它要解决的（丢弃被打断那一轮的在途音频）是**单轮**的。F18 修掉了已知的回退路径，`docs/46` 让丢弃不再静默；残留的是这个错配本身 —— 将来任何序号回退都会以同样的方式复发。见下方「P1-7 备注」 |
| **P1-8** | **badge 的时钟与音频的时钟也是独立的** | 徽章可见窗口 `visibleWindowSeconds = 4.0`；F19 之后播放不再被 20s 超时剪断，长回复会**盖过徽章的整个显示窗口**。另有两条既有的 turn 归属问题待验。见下方「P1-8 备注」 |

> **以下 P1-12 ~ P1-23 来自 WSS 系列全盘扫描（2026-09-11）**，逐条对照代码核实，详见 meta `docs/30_技术方案/81`–`94`（尤其 `90` 实践总结与 `88` 失效模式）。按性价比排序：**①②③ 是小改动、无联动、收益明确**。

| # | 问题 | 影响 |
|---|---|---|
| ~~**P1-12**~~ ✅ | ~~客户端遇到未知帧类型会断开连接~~ | 服务端已"忽略并计数"，但客户端 `WSControlFrame` 解码遇到不认识的 `type` 会**抛错 → 发 `.disconnected` → 退出接收循环**。后果：一个只加了新帧类型的服务端版本，**能让所有老客户端掉线** —— 与当年 `unsupported_frame` 打死活会话是同一类事故，只是搬到了另一端。**已修**（ios `cb7da37` + `ios docs/52`）。根因不是解码器的契约而是**传输层的反应** —— `decode` 抛 `unknownType` 没有错（它造不出一个帧来返回），错的是调用方把它和"帧坏了"合并成同一个 `decodingFailed`。所以只对 `unknownType` 提前返回，**解码器一个字没改**，既有测试零改动。跳过留痕（tracker 事件 `transport_control_frame_ignored`），边界守住（信封坏掉的帧仍致命）。红验证两次，第二次是关键：把兜底 `catch` 改成 `return`（模拟偷懒的"全跳过"修法）→ **只有边界守卫红**；没有它，偷懒修法能让复现测试全绿通过。顺带开了一条测试缝 `SocketMessageSource`（P1-22 的一部分）。`swift test` 450/450 |
| ~~**P1-13**~~ ✅ | ~~`receiveLatency` 量错了区间~~ | 起点采样在 `await` **之前**，量到的主要是"socket 干等下一帧"，`handle()` 的开销完全没计入；而文档注释描述的是另一回事。一次爆发边界得到大样本、爆发内部全是近零 —— **数字一直看着合理**。**已修**（ios `c5f5cab` + `ios docs/53`）。**同一个标记三处都测错了对象**，台账只记了第一处：①起点采样在 `await` **之前** —— 空闲 120ms 的连接读出 **128.21ms**；②终点采样在 `handle()` **之前** —— 8MiB 音频帧读出 **0.029ms**；③`frame_type` 取的是 JSON **键**不是值 —— 每控制帧恒为 `"type"`（`{"ts":7}`→`"ts"`，`not json`→`"ot json"`）。第 ③ 处**是"先写测试"直接兑现的收益**：单看那段扫描代码它读起来像"取第一个引号里的东西"，只有把期望值 `"ping"` 写下来才当场显形。三次红验证里 A/B 两次**互补保持绿**，证明两条测试各自守住区间的一端。**影响面**：此前所有基于 `timing_socket_receive` 的读数都不可用。`swift test` 453/453 |
| **P1-14** | **打断没有在服务端留下任何痕迹** | 网关收到 `interrupt` 后两层各记一条日志即止：**不停止转发该轮剩余音频，也不在会话记录里标记**。后果：**持久化的转录记录了用户从没听过的话** —— 对一个把转录当学习资产的产品。**不需要上游配合**（上游无取消能力，但这里要的是记录的正确性，不是省算力） |
| ~~**P1-15**~~ ✅ | ~~两条"有消费、有测试、没有生产者"的死路~~ | ① `processingReview` 相位：进入它所需的事件**生产代码从不派发**，全仓唯一派发在测试里；② "重连成功"：`.reconnectSucceeded` 同样没有生产者，**每次网络丢失都必然落到文本降级**，那个 3 秒窗口是在等一个不可能发生的事。**已处置：标记 + 钉住，未删除**（ios `8f9e594` + `ios docs/55`）。**核实改变了两条的处置方式 —— 两条都补不上，所以"接线"不是选项**：①`review` 的信号只能来自网关，而网关 `stage` 词汇里没有它（P1-20 是同一缺口的另一面）；②**接不了** —— 网关无法恢复会话（见下）。**运行时行为零改动。** **②的关键更正**：那个 3 秒窗口**不尝试** —— 它只 sleep 然后派发 `reconnectTimedOut`，**从不重开 socket**，所以"每次网络丢失必然落到文本降级"成立，且"3 秒窗口内自动重连"里只有"3 秒"是真的。死掉的不是"一个事件"而是**七个 artifact**（事件本身、`socketReady`-while-reconnecting 分支、`Configuration.reconnectWindow`【声明了从不读】、`SocketConnectionState.reconnecting`【从未发出】、窗口本身、`completeReconnect`、以及语义是"窗口"实为"降级延时"的 CancellationID）。**后端核实（为什么 iOS 单边接不了）**：要"携 `session_id` 恢复"需要三样、一样都不存在 —— 能携带 session_id 的帧（`auth` 只有 `ticket`，`additionalProperties: false`）、会话查找（全包无注册表，状态活在连接断开即丢的 `sessionRuntime`）、活上下文持久化；且票据一次性，重放 → `ErrTicketUsed`，换新票 = 新 uuid。**这是一张独立的后端票，不是 P1-15 的修复。** 守卫 `networkLossDegradesAndNeverAttemptsAReconnect` 钉住"不重连"（`startSessionCallCount == 1`），让将来实现重连成为必须 deliberate 的事。 |
| **P1-16** | **生产路径没有"音频结束"信号** | `ai.tts.end` 在整个后端**只有本地开发的 mock 会发**。客户端无法从协议上知道"AI 说完了"，只能观察到"音频不再到达" —— 而"说完了"和"卡住了"长得一模一样。实现者会用**计时器去逼近一个信号**，而两者不等价。需客户端联动，非单边决定 |
| **P1-17** | **保活与服务端的空闲回收互相抵消** | 客户端每 30s 的应用级心跳，让网关那个 **2 分钟读空闲超时永远不会触发**。两个各自合理的设计合起来让其中一个失效，而**没人负责发现** —— 每一半单看都对。**两样都留着、但一个是装饰**，是最糟的第三种状态 |
| **P1-18** | **冻结的 v1 里有两处从没跑过的死面** | `ai.audio.chunk` 帧类型与 `interrupt.max_seq` 字段：后端从不生产也不消费，却已**字节冻结**在 v1 —— **现在删不掉**。教训：**冻结之前先跑一遍**；承诺一个没验证过的东西，是把未知变成负债 |
| ~~**P1-19**~~ ✅ | ~~错误码 → 用户文案只覆盖 12 个里的 5 个~~ | **已修**（ios `8fb43cf` + `ios docs/57`）。最刺眼的是四个**同族**上游错误（`provider_audio_failed`/`control_failed`/`open_failed`/`start_failed`）前三个有文案、第四个没有。**为什么没有测试发现**："少一条"不是错误 —— 兜底分支总能产出字符串。补齐 12 个码 + 兜底改成**人话在前、标识在后**（未知码仍带 `code`，但作为人话的附录而非替代）+ 参数化测试跑全部 12 个码。**共享 schema 有意不枚举 `code`**（加 `enum` 会让每个新码变成 schema 违约，正是 P1-12/P1-20 那类前向兼容陷阱），所以修法不能是把码表塞进 schema。**顺带发现 `unsupported_frame` 已废弃**，只在 `handler.go` 注释里说明"不再发它" |
| **P1-20** | **阶段标签号称是跨服务日志契约，实际只兑现 3/13** | 客户端 13 个相位标签里只有 3 个在后端有对应生产者。在客户端日志里看到某个标签，会**以为**能在服务端搜到同名的东西 —— 搜不到且不报错。这也改变 P1-3（相位收敛）的前提：它面对的是一个只兑现了 3/13 的契约 |
| **P1-21** | **`server_ts_ms` 只挂在 `ai.text.delta` 上** | 只有**文本**首帧能拆成上行/服务端/下行；而**音频才是用户感知的那一路** —— 它的首响永远只有一个总数，拆不开 |
| **P1-22** | **这一层的测试空白：没有任何测试构造过真实 socket** | 连接**建立**（含握手时序）、接收循环的**生命周期**、取消/重连/析构、真实网络错误形态 —— 全部未覆盖，只可能在真机第一次暴露。另：`InMemorySocketTransport` 共享了丢弃**判定**，但**没有**共享丢弃**上报** —— 而"丢弃是否可观测"正是排查时首先要看的 |

| ~~**P1-23**~~ ✅ | ~~设计文档称"后端网关无状态、上下文在 Redis"，网关侧零 Redis 依赖~~ |`internal/`、`cmd/`、`pkg/` 无 Redis import，`go.mod` 无 Redis 依赖；Redis 只出现在 `deploy/docker-compose.yml` 与 README / 脚本里 —— **是基础设施与文档，不是代码**。真正的位置是 **MySQL**，且存的是**记录不是可恢复状态**（`practice_sessions` / `utterances` / `session_tickets`），没有活会话上下文、音频水位线、provider 句柄。后果：`32_` 用它论证了"3 秒窗口内自动重连 + 携 `session_id` 恢复"这个**从未实现、且当前接不了**的能力（见 P1-15）。与 P1-19(5/12)、P1-20(3/13)、P1-18 同族 —— **单看每一处都像活的**。**已修**（meta `docs/30_技术方案/32`）：原第 4 条划掉，附"已知偏差"表列出三处声称 vs 实际（窗口不尝试重连 / 协议无处自报 session_id / 上下文不在 Redis 而在 MySQL 且是记录不是状态），并写明补齐所需三样。**保留划掉的原文而非删除** —— 它是"设计写了、实现没做、文档还引用着"的样本 |

#### P1-7 备注：播放门禁水位线的生命周期（保留上下文）

**客户端有 2 道水位线，彼此独立，且都只在会话级重置：**

| 门禁 | 位置 | 置位 | 清除 |
|---|---|---|---|
| `AudioPlaybackGate.interruptWatermark` | `LiveAudioEngine` | `interruptNow()` | `startCapture()` / `stopCapture()` |
| `AudioFrameDropGate.interruptMaxSequence` | `URLSessionSocketTransport` | `transport.markInterrupted()` | **只有 `connect()`** |

第二道的 `clearInterrupt()` **在生产代码里零调用**（只有定义和测试）—— 设下后整个连接期间永不清除。

**2026-09-11 实测复发**：真机「播放不全，有时前半段、有时后半段」。成因是第二道门禁 + 当时尚未部署的 F18：barge-in 设下水位线 W → 网关重开把序号从 1 重来 → 传输层把新一轮的 1..W 全丢在到达引擎之前。**不需要滚动**，触发条件是重开。

**现状**（`fluentwork-ios` `LiveAudioEngine`）：

```swift
struct AudioPlaybackGate {
    private(set) var interruptWatermark: UInt32?    // 由 interruptNow() 设下
    mutating func shouldAccept(_ frame: WSAudioFrame) -> Bool {
        if let interruptWatermark, frame.sequence <= interruptWatermark { return false }
        ...
    }
}
```

它**只在 `startCapture()` / `stopCapture()` 重置** —— 即整场会话级别，从不按轮清理。

**它要解决的问题是单轮的**：打断（barge-in / 系统中断）发生时，把**那一轮**已经排进播放队列的在途音频丢掉。这个目的是**轮次范围**的，而实现给了它**会话范围**的生命周期。这个错配就是全部风险来源。

**已修的部分**（F18）：后端透明重开曾把序号从 1 重来，撞上停在几百的水位线 → 之后每一帧被**静默**丢弃。表现是「文字照常返回、音频永久消失、不报错」。后端已改为跨重开接力编号。

**残留**：水位线的生命周期仍然错配。将来任何别的序号回退路径（新的重开路径、新的 provider、协议改动）都会以**完全相同**的方式复发 —— 而且是静默的。

**修法方向**：让水位线**按轮失效**。打断那一轮真正结束时清掉它。需要一个可靠的轮次边界信号，`ai.turn.end`（带 `turn_id`）是现成的候选，但它目前没有喂给播放门禁这条路径。

**明确否决的做法**：客户端加「序号看起来变小了就重置」的启发式。门禁逻辑本身没错，错的是它依赖的前提；启发式会在**真正的乱序场景**下帮倒忙，而且会把真 bug 藏起来。

**已做（可观测）**：`docs/46` 让两道门禁的丢弃都留下记录 —— `SocketTransportDiagnostic.audioFrameDropped` + tracker 事件 `transport_audio_dropped`，报告规则抽成 `AudioDropReport` 纯类型。`sequence <= watermark` 却出现在设下水位线那一轮之后，就是序号回退的签名。判据从"猜"变成"读"。

---

#### P1-8 备注：badge 与音频的时钟（保留上下文）

**先说清楚 F19 改了什么、没改什么** —— 免得误以为徽章逻辑被动过。

`904316f` 只动了**一个转移的副作用列表**：从 `(.waitingForEvaluation, .evaluationTimedOut)` 移除 `.stopPlayback`。以下**全部未动**：

| 组件 | 现状 |
|---|---|
| `feedback.badge` 帧的处理 | `badgeHit` 与 `evaluationReceived` **无条件派发**（中间件 480-500） |
| 徽章渲染 | `BadgeFeedbackFeature` 按**时间**过滤，不看相位 |
| 状态机的 `evaluationReceived` | 只在 `.waitingForEvaluation` 有分支（机器 66 行） |
| `EvaluationArrivalBox` | 未动 |

**真实耦合：两个时钟又碰上了。**

```
评估窗口   evaluationWait        = 20s    （固定预算）
徽章可见   visibleWindowSeconds  = 4s     （固定预算）
播放       ── 由 AI 说了多长决定 ── 无上界
```

在 F19 之前，20s 超时会把音频剪断，所以"徽章出现时房间是安静的"。现在音频继续，**长回复会盖过徽章 4 秒的整个显示窗口** —— 用户注意力在听，可能根本看不到徽章。这和 F19 是**同一类错误**（用一个固定时钟去管一件长度由内容决定的事），只是方向相反。

**三条待验（按可验证性排序）：**

| # | 事项 | 怎么验 |
|---|---|---|
| **a** | **徽章 4s 窗口 vs 播放长度** | 真机：让 AI 给一段长回复并命中徽章，看是否能在听的同时注意到徽章。**判据是"用户看得见吗"，不是日志** |
| **b** | **超时后到达的徽章，状态机不认** | 机器没有 `(.waitingUser, .evaluationReceived)` 分支。徽章**会显示**（`badgeHit` 无条件），但机器不把"评估已到"记下来。影响面待定 —— 需查它是否喂给 turn outcome / 回顾 |
| **c** | **`EvaluationArrivalBox` 是单槽布尔，不按轮** | `mark()` 置位、`consume()` 清位，只在会话起止 `reset()`。徽章若在机器已离开 `waitingForEvaluation` 后到达，标记会**残留到下一轮**，使下一轮进入该相位时 `consume()` 返回 true → **跳过 20s 计时器**。这是既有问题，F19 没引入，但值得验 |

#### 验证前提：真机必须有自己的语料（否则徽章永远不会来）

**已实测踩到**：`dev-up.sh` 的自动灌库写死在 `device_id=corpus-seed-dev-device`，而**真机永远不是这个 id**。语料按 `user_id` 隔离（`GET /internal/v1/corpus/blocks?user_id=`），所以真机那边语料是空的 → `feedback.badge` 永不出现 → **命中徽章根本无从发生，而且没有任何报错**。

真机 `DA87E7D4-1371-4984-AD3F-D6D70B4D17D2` → user `fe7a5b1a-…`，灌库前 `{"items":[]}`，灌后 10 条。

```bash
# 给真机自己的 device_id 灌语料
go run ./cmd/corpus-seed -device-id <真机 device_id>
```

**灌进去的 10 条锚点（说左边，命中右边）：**

| 说 | 命中徽章 | 意图 |
|---|---|---|
| I am blocked on the API review. | I'm blocked on the API review. | 说明阻塞点 |
| how about we pair on this tomorrow | How about we pair on this tomorrow? | 主动提议 |
| I don't want to push the deadline | I'd rather not push the deadline. | 委婉拒绝延期 |
| thanks for your time | Thanks for your time today. | 礼貌结束 |
| let's ship it | Let's ship it. | 推动上线 |
| could you clarify | Could you clarify what you mean by that? | 请求澄清 |
| does that work for you | Does that work for you? | 请求反馈 |
| I'm not sure yet | I'm not 100% sure yet, but I'll confirm by EOD. | 承认不确定 |
| let's wrap up the meeting | Let's wrap up. | 把会议收尾 |
| bottom line | Bottom line: we'll ship next Tuesday. | 总结结论 |

**注意**：DEBUG 版的注入徽章按钮走的是 `badgeHit` 直通路径，**绕过了后端检测**，所以它一直能用 —— 它验的是渲染，不是命中。两者要分开看。

**本票不做**：只记录。b、c 需要先定性（是否真的有害），再谈修法；a 是产品判断（窗口该多长），不是 bug。

### ~~P1-9~~ ✅ — 测试基础设施：`.singleton` 工厂的注册会跨测试泄漏

**已实测踩到**：`processingTimeouts` 是 `.singleton`，一个测试注册 80ms `connectWait` 后，`transitionTelemetryEmittedOnPhaseChange` 与 `ttsStreamEmitsStartFirstAudioAndEndViaSharedTracker` **5 次里挂 3 次**，单独跑却 4/4 全过。

**机制**：`.singleton` 让注册变成**进程级**粘性 —— 注入的值泄漏给所有**并发**运行的测试。测试里 `defer` 还原只在测试结束后生效，救不了同时跑的；两个受害者在另一个 suite，所以 `.serialized` 也救不了。

**已修**：`processingTimeouts` 去掉 `.singleton`（无状态值类型，缓存本就没有意义）。5/5 全绿。

**同类风险仍在** —— 另有 4 个 `.singleton` 工厂被测试 `register`：

| 工厂 | 泄漏症状 |
|---|---|
| `corpusCacheStore` | 别的测试拿到你这个假 cache |
| `corpusOutboxStore` | 同上 |
| `corpusSyncMetadataStore` | 同上 |
| `networkMonitor` | 别的测试拿到你写死的网络状态 |

机制与 `processingTimeouts` 完全相同，只是注入的是**有状态替身**而非纯数值，所以症状是「拿到别人的假对象」而不是「时序被静默改变」。**还没撞出可见失败，但风险是同一类。**

**已修**（ios `4821184` + `ios docs/56`）。**缓存作用域语义已确认，且比本行的措辞更彻底**：`Scope.Singleton` 的注释是字面的 —— *"ignore container's cache in favor of our own"* —— 它用的是**自己的**缓存，而 `self` 是共享的 `Scope.singleton`。所以 `Container()` 是不是新的无关紧要、`container.reset()` 清不掉、`defer` 更没用。**不是"单例缓存破坏隔离"，是单例作用域根本不在容器里。** 所以有状态替身不需要额外的隔离机制，它需要的是**换一个作用域**。

修法：`.singleton` → `.cached`（`Cached` 不覆写 `resolve`，用的是传进来的容器缓存，且 `reset()` 会清掉它）。**范围是全部 25 处，不只是本行点名的四个** —— 因为生产代码永远走 `Container.shared`：`AppStore` 与四个 Middleware 工厂的签名都是 `container: Container? = nil` → `container ?? Container.shared`，而**生产调用点一个都没传**。那个参数存在的唯一目的就是让测试注入新容器，而 `.singleton` 恰好废掉了这个注入。**这不是宽改动，是让 DI 的设计意图成立。**

守卫写成**性质**（两个独立容器不共享实例）而不是点名，将来新加的工厂若也用 `.singleton` 照样抓得住。**顺带撞出一个编译器 bug**：`(existential as AnyObject) !== (existential as AnyObject)` 让 Swift 6.3.3 直接崩（signal 6，不是类型错误），需先转型到具体类型 —— 看起来像自己的代码写错了，实为工具链问题，已记档。`swift test` 458/458 |

---
### F21 备注：同形状未处理的两处（保留上下文）

**一、音频引擎那条消费者：已修（同一票的第二半）。**

第一版把它留在"不做"里，理由是"它的状态是会话级的"。**真机立刻证伪** —— 重进修好之后点「开始说话」没有任何反应：`beginManualSpeech()` 到达了引擎、引擎发出了 `.speechStarted`，但那个流没人读，机器停在 `waitingUser`。

**理由错在哪**：`pcmBuffer` / `isCapturingSpeech` 确实是会话级**语义**，但它们恰好是**安全的跨会话残留**（`.speechStarted` 里先 `pcmBuffer.removeAll()` 再 `speechCaptureGate.beginSpeech()`，顺序保证上一会话的 PCM 到不了线路）。**语义是会话级的，不代表持有它的循环必须是会话级的。**

已提取成 `audioEventPump`，与传输泵一起在"第一次要会话"时启动、store 级、永不取消。测试 `audioReaderSurvivesSessionEnd` 先红后绿。

**二、官方研究里两条我们尚未处理的实践**（来源见 ios `docs/49` §5）：

| 项 | 现状 | 风险 |
|---|---|---|
| `URLSessionWebSocketTask.maximumMessageSize` 有默认上限 | 未设置 | 大消息会在接收回调里以**误导性的网络错误**失败，而不是明确的"消息过大" |
| 断线**无内置重连**：`receive()` 一旦返回错误，task 即终结 | 由 `connect()` 重建 task | 重连路径未经真机验证 |

---

### P1-10 — 评分器缺归一化，WER 偏悲观 3-4 个百分点

**已实测**：30 条样本的 39 个错误里，**10 个是纯格式差异** —— 识别器听对了词，只是写法与参考不同（`3:30` vs `three thirty`、`I have` vs `I've`、`back end` vs `backend`）。归一化后是 29 处 / 10.4%，而工具默认输出 14.7%。

**风险**：这个数字会被引用。偏悲观 3-4 个百分点不会让这次结论反过来（10% 和 15% 指向同一个决定），但**下一次运行会被同一个偏差污染**，而没人会记得要减掉它。

**为什么没顺手改**：归一化的**口径本身会改变数字** —— 哪些算等价（数字？缩写？连字符？同义词？）需要单独定义并留档，否则只是换了一个新的、同样不可解释的数。

**修法方向**：加一层显式的、可测试的规范化，**同时报两个数**（原始 + 归一化），而不是替换掉其中一个。

---
### ~~P1-11~~ ✅ — 读错误分类过宽：`context.Canceled` 被当成"上游连接已死"

`voicepoc.collectTurn` 的读错误只区分 `context.DeadlineExceeded`，**其余全部**包成 `ErrDuplexClosed`：

```go
if errors.Is(err, context.DeadlineExceeded) { /* 优雅降级 */ }
else { collectErr = fmt.Errorf("%w: %w", ErrDuplexClosed, err) }   // ← context.Canceled 也走这里
```

而 provider 拿到 `ErrDuplexClosed` 就 **reset 整个 duplex**，丢弃服务端会话上下文。

**为什么算缺陷**：同一份代码在另一处的注释里明确写着立场 ——「provider error 事件不同于连接死亡，reset 会丢弃服务端上下文」。分类把**调用方主动取消**和**套接字真的死了**混成一类，与那个立场不一致。

**影响面**：生产上 `HandleClientControl` 拿到的是 WSS handler 的 ctx，客户端断开时取消 —— 那种情况下 reset 无害（会话本来就要结束）。所以**不是紧急缺陷**，但它让"哪些情况该 reset"这个问题失去了精确答案。

**已修**（backend `da60576` + `docs/57`）。新增 sentinel `ErrTurnCancelled`，把取消从"断连"里摘出来 —— `ErrDuplexClosed` 的语义因此**变纯**：只表示"套接字死了"，正是 `resetAfterTurnReadFailure` 该被调用的那一种；调用方 `errors.Is(err, voicepoc.ErrDuplexClosed)` **一个字没改**就自动不再对取消重置。

**为什么算缺陷（比本行的措辞更准）**：就在那个 `if` 上方三行，注释写着 *"A provider error event is different: the session is still usable and resetting would discard the server-side conversation context"* —— 这份代码**已经**在按"会话还能不能用"分类，而且明确把"重置会丢弃服务端上下文"当成代价，却又让 `Canceled` 走进重置分支。**分类粒度与它自己的立场对不上**，而不只是"分类过宽"。

红验证抓出的实际输出：`canceled` 被逐字包成 `duplex connection closed: failed to get reader: context canceled` |

**发现于**：步 6 排查时（写测试时用了短 ctx，撞出了这个分类）。

---
### P2 — 待核实(不要用文档猜)

| # | 事项 |
|---|---|
| **P2-1** | 模型版本:`1.2.6.0`(我们配置)vs 官方文档固定值 `1.2.6.1` —— 回控制台核对 |
| **P2-2** | 成本口径:官方计费文档**自身矛盾**(输出-文本一处 80 一处按 30 算,差 2.67 倍)—— 以账单为准 |
| **P2-3** | `meta #12` 商务三项(免费额度 / 数据不训练 / 并发配额)未闭环 |
| **P2-4** | Prod 侧 TTS SKU 授权(曾有 403 / `code=55000000`) |

---

## 3. AEC 怎么做

**先说结论:主方案是 Apple 的 Voice-Processing I/O,而它现在很可能没真正生效。**

### 3.1 三条可选路径

| 路径 | 做法 | 代价 |
|---|---|---|
| **① Apple Voice-Processing I/O(主)** | 系统级 AEC + 降噪 + AGC,免费、无包体、不引第三方 | 需要正确启用 + 实测;有已知的格式副作用 |
| **② 半双工兜底** | AI 播放时**停止上行麦克风** | 简单、必定有效。但**牺牲 barge-in**(用户不能打断 AI) |
| **③ 官方 SDK 的 AEC** | SDK 内置,需配 `aec.model` 模型文件 | **与本仓自研 + 零 key 约束冲突**,只在 ① 实测不行时评估 |

### 3.2 ① 为什么可能没生效 —— 两套 API 要分清

| API | 现状 | 说明 |
|---|---|---|
| `AVAudioSession` category `.playAndRecord` + mode **`.voiceChat`** | **已配置** ✅ | 这是**会话级**的 VPIO 开关 |
| `AVAudioEngine.inputNode.**setVoiceProcessingEnabled(true)**` | **未配置** ❌ | 这是**引擎级**的开关(iOS 13+)。用 `AVAudioEngine` 自建图时,这一条往往才是决定性的 |

当前 `DefaultAudioSessionManager` 只做了前者。**这两者不等价** —— 只设 category 而不用引擎级 API,在自建 AVAudioEngine 图里 AEC 未必生效。

**已知副作用(改动前要预料到)**:开启 input node 的语音处理会**改变输入节点的格式**,可能让现有的 tap/`AVAudioConverter` 链失效。所以这一步要和 `convertToPCM16` 的格式断言一起改。

### 3.3 怎么**验证** AEC(不能靠"听起来没回声")

**判据是可观测的,不是主观的:**

| 信号 | 说明 |
|---|---|
| **转录里出现 AI 自己刚说过的话** | AEC 失效最直接的证据 —— 麦克风把扬声器放的 AI 语音录进去,被 ASR 转成了用户输入 |
| **未开口却触发新一轮** | AI 的声音被当成用户说话,触发 server VAD |
| **`interrupt` 自触发** | AI 播放中被自己的声音打断 |

**具体做法**:真机上让 AI 说一段**含独特短语**的话,用户**全程不出声**。

- **AEC 生效** → 该轮 transcript 为空 / 无新一轮
- **AEC 失效** → transcript 里出现那个独特短语

这个测试**必须在 P0-1(AI 出声)之后立刻做**,而且可以自动化断言(比对 transcript 与 AI 上一轮文本)。

**判定工具已就位**：`fluentwork-backend/scripts/check-aec.py`（backend `3f5b2be`）。

```bash
# 真机上静默一轮之后
./scripts/check-aec.py /tmp/fluentwork-backend.log
# exit 0 = 无自激证据；1 = 发现自激
```

它逐会话比对 `transcript` 与上一轮 `assistant_text` 的重合率。分词带**中文二元组** —— 这不是优化是必需:本产品大量会话是中文,纯 ASCII 分词会把每条中文转录判成"空"而跳过,恰好跳过这个脚本存在的意义所在的那批会话。

**已在现有日志上跑过**：10 会话、0 自激。但那批会话用户一直在说话,**回答不了 AEC 问题** —— 弱阳性,不是结论。

### 3.4 失败时的退路

若 ① 实测不达标:**先上 ② 半双工**(播放时停上行),把 barge-in 暂时降级为按钮触发 —— 产品上可接受(`docs/41` 已把交互改成"点一次开始"),工程上立刻消除自激风险。**③ 留作最后手段**。

---

## 4. 执行计划

**顺序有依赖,不要跳步。**

| 步 | 做什么 | 依赖 | 验收 |
|---|---|---|---|
| **1** | **让 AI 先出声** —— 网关转发 `response.output_audio.delta`,24k→16k,按 ~100ms 切二进制帧,**不发 `ai.tts.start`** | 无 | 真机听到 AI 说话;本地 mock duplex 断言帧序列 |
| **2** | **同一动作里验 AEC**(§3.3 的"独特短语"法) | 步 1 | 用户不出声时 transcript 为空 |
| **3** | 若 AEC 不达标 → 半双工兜底 | 步 2 | 无自激 |
| ~~**4**~~ ✅ | ~~**语音成本记账**(P0-3)~~ | 无 | `ai_cost_logs` 出现 `voice.duplex` 行；**能算出单次练习用了多少音频秒**。算钱等费率核实（P2-2）|
| ~~**5**~~ ✅ | ~~**ASR WER 抽样**(P1-4)~~ | 无 | **已测**：**14.7%**（归一化后 10.4%），`D=0`。**结论：不迁移到分解流水线** —— 失败模式是替换而非切分，换管线用同一家 ASR 会原样保留。要压替换应**先测另一家 ASR**。工具链 + 30 条诊断性样本见 `docs/52` |
| ~~**6**~~ ✅ | ~~补 `input_audio_mute.commit`(P0-4)~~ | 无 | 代码与测试已齐（`762d26e`）。**待真机**：两轮间隔 > 60s 仍正常 |
| ~~**7**~~ ✅ | ~~B17 接上或标记未启用(P1-1)~~ | 步 1 之后可定 | **决策：标记未启用，不接线**（backend `79ded15` + `docs/54`）。消费者（每日一读 `AudioURL` 无生成者、闪测只是 tab）与生产授权（P2-4 仍 403）两样都没有；接线只会得到"dev 通过、prod 失败"。**P1-1 关闭，P2-4 保持开启**（外部阻塞，厂商侧授权）。重新评估条件：**消费者到位 且 P2-4 关闭**，两者同时满足 |
| ~~**8**~~ ✅ | ~~真流式(P1-2)~~ | 步 1 | **文本半边达成**（backend `e3fc02a` + `docs/55`）：`ai.text.delta` 已是真增量。**音频半边未做，且排查中确认它更大** —— 整轮音频攒完才发，AI 要等全部 TTS 生成完才开口；需要带状态的重采样器，见 `docs/55` §7。**建议作为下一步单独立项**，不要与相位收敛混做 |
| **9** | 相位收敛(P1-3) | 步 8 之后 | **设计已出，未实现**（ios `a50fea9` + `ios docs/50`）。**未实现是刻意的** —— 面 ~62 处引用，动的是 4 个真机缺陷里 3 个出在其中的状态机；留半个收敛的状态机比现状更难推理。设计：13 → 9，**分两次提交**，提交一（13→11，机械重命名）验收"测试一条不少"，提交二（11→9，语义合并两个等待）验收同前，**F19 的守卫测试（评估超时不停播）必须原样通过**。实现前先确认后端三个 stage 串取值 |

**明确不做(现在)**:把端到端 duplex 换成分解流水线。方向见 `74_` §2,但**基线不存在** —— 连"AI 出声"都没有的时候换架构,等于同时改两个变量。**步 5 的数字出来后再决定。**

---

## 5. PRD 层问题(来自 2026-09-11 评审)

评审原文肯定 §2 的第一性原理拆解与 §14.3 的护城河论证;以下为可调整项,按严重度排序。

> **状态（2026-09-11）：D1–D7 已全部并入 PRD V1.6**（PRD 修订记录 + 启动书 V1.2）。UX 纲要另立为 `docs/20_产品设计/23_理想UX交互纲要.md`。
>
> ⚠️ **D1（B8）是 MVP 范围增补**——按启动书 §四 需 **CTO + PM 会签后生效**。会签前视为**已评审待批**，排期占用见 PRD 第十章 W5。

| # | 问题 | 严重度 | 落地位置（PRD V1.6） |
|---|---|---|---|
| ~~**D1**~~ ✅ | **设计原则第 4 条「干预分层」无对应功能需求** —— 翻遍需求详表无任何 ID 实现;B 模块没有"用户沉默/卡壳时的救援机制" | **最严重** | **§5.4 完整机制 + B8（P0）+ 第十章 W5 + 原则 4 补约束**。另**为 B8 自己记了一条风险**（「卡壳救援变成依赖」——梯子给太早会与主动召回目标相反），见风险表 |
| ~~**D2**~~ ✅ | **闪测 E2 有未覆盖的误判闭环** —— 闪测是短语音,ASR 错误率天然更高;转错 → 判"语义不等价" → **用户说对了却判失败**,打击"主动召回"训练 | 高 | **E2 增加"判定前展示识别文本 + 一键申诉"** + 风险表新增「闪测误判：说对了却判失败」 |
| ~~**D3**~~ ✅ | **B7 误命中风险未入风险表** —— 语义判定必然有误命中;错误正强化在教学上是负资产,用户发现一次即信任崩塌 | 高 | **B7 加"阈值保守化，宁可漏报不可错报"+ 风险表「B7 误命中 → 错误正强化」**（原风险表只防了"打断节奏"） |
| ~~**D4**~~ ✅ | **北极星门槛与会话时长矛盾** —— "周完成 ≥1 次完整流水线"约需 15 分钟,对碎片时间用户结构性压低完成率,且分不清是"没价值"还是"太重" | 中 | **§三 口径补充 + B1 + 信息架构创建弹层 + 第十一章**。迷你会话（3-5 回合，约 2 分钟）计入北极星 |
| ~~**D5**~~ ✅ | **漏斗缺最关键的先行指标** —— 断点在"进入房间 → 说出第一句话"(开口恐惧是核心画像的默认状态) | 中 | **第十一章新增「漏斗首环」与「救援」两行**：首次开口率 + 首次卡壳救援触发率，并写明两者**配对读**的方式 |
| ~~**D6**~~ ✅ | **AI 语速调节缺失** —— 目标用户"读写过关、听说薄弱",听不懂常是语速问题而非词汇 | 中 | **G2 + 信息架构设置页**：0.8x / 1.0x，定位为**可懂度开关**而非舒适度选项 |
| ~~**D7**~~ ✅ | **归档**:PRD §9.1 写的是「ASR→LLM→TTS 级联」,与现网端到端 duplex 是**两个物种** | 已知 | **§9.1 口径归档表**：列出级联/duplex 在形态、中间产物、B7 依赖、成本路由四维的差异，并明写"这是**已知偏差**不是笔误"。与 `74_` §2 同源 |

### UX 纲要(评审推演,供设计文档对接)

> **已立为独立文档**：`docs/20_产品设计/23_理想UX交互纲要.md`（V0.1，待评审）。下表是摘要。
>
> **注意其中两条尚未采纳，不要按已定案读**：**「关键词镜像」会改变 PRD B3 的字面要求**（现为"实时转录浮层"），**「语料库按未来会议组织」会改变 Tab 3 首页结构**（F1/F2 功能点不变）。两条都需产品评审 + UI 出形态。其余条目为对既有方向的展开，不改变 PRD 要求。

**第一公理**:说话的时刻,把 100% 认知带宽留给"说";**所有需要"阅读"的东西后置到回顾**。

| 区域 | 理想形态 |
|---|---|
| **说的房间** | 语音是唯一主线;**实时转录应是"关键词镜像"而非逐字稿**(逐字稿会诱导从"说"切到"读",认知过程就断了);**对话中只强化不纠正**,纠错只在回顾页;**卡壳救援是对话流内唯一允许的干预**,形态是给梯子不是给答案 |
| **回顾页** | 第一屏只放一件事 —— **"你刚才最值钱的一次升级"**:原句(可回听自己的声音)→ 地道版(AI 朗读)→ 一键入库。三层评价/问题清单全部后置。等待用"高光与卡壳波形回放"填充 |
| **闪测** | 答对反馈**即时且轻**;结算页文案不是"成功率 80%",而是"**'降级'这个词,你已经能脱口而出了**" |
| **语料库** | **按"未来的会议"组织,不按标签**:"下周一 Standup 你可能用到的 5 句话" |

**一句话**:把"刻意练习"伪装成"一次顺利的对话";所有"训练感"发生在对话之外。

---

## 6. 本清单怎么用

1. 每修一条,在对应行标 ✅ 并补上提交哈希与实现说明链接
2. **一次只推进一条**,跨仓串行(后端与 iOS 不同时改)
3. 新增问题直接进表,不另开文档
4. 表变长不是坏事 —— 变长且没人动才是
