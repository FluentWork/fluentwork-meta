# FluentWork 问题总清单

**日期**：2026-09-25
**定位**：**唯一入口**。把产品、架构、后端、iOS、以及 PRD 评审五路来的问题汇成一张可逐条执行的表。逐条修、逐条勾。
**本文是索引。** 条目内容按**章节**拆在三个子文件里（见 §3），本文给**按阶段**的视图（见 §2）。

**本文取代三份来源**（2026-09-25 合并）。三者下场不同，注意区分：

| 原文件 | 原编号体系 | 现在的位置 | 原文件状态 |
|---|---|---|---|
| `meta/docs/40_研发流程与协作/77_待修复问题总清单_2026-09-11.md` | `F1`–`F21` / `P0-x` / `P1-x` / `P2-x` | [`问题总清单-产品与缺陷.md`](./问题总清单-产品与缺陷.md) | **仍在**，已加「被取代」横幅（保留日期快照） |
| `fluentwork-ios/docs/80_架构分析/05_问题清单与建议.md` | `S0-x` / `S1-x` / `S2-x` | [`问题总清单-iOS架构.md`](./问题总清单-iOS架构.md) | **已删除**（`80c8209`） |
| `fluentwork-backend/docs/104_架构分析/05_问题清单与建议.md` | `S0-x` / `S1-x` / `S2-x` | [`问题总清单-backend架构.md`](./问题总清单-backend架构.md) | **已删除**（`e2c61f7`） |

**为什么合并**：三份各自完整、互不引用，而其中自称「唯一入口」的那份停更了 13 天。
结果是一个「待办」有三个归属地，且没有任何东西会在它们分歧时变红 ——
这正是本仓反复撞到的形状（`AGENTS.md` R2；backend `docs/108_` 记的「一个不变量有两个归属地」）。

**为什么按章节拆而不是按时间拆**：活文档名字带日期，正是它被孤立的机制 ——
读的人以为「这是 09-11 那版，应该有更新的」，然后去别处找。三份来源里那份停更的，
文件名就带日期。**快照**（如 `39_后端技术架构设计V2_0_2026-09-03`）带日期是对的；
**活文档**不带。

> **确定性标记**：【实测】= 命令/输出可复现；【读码】= 源码字面；【推断】= 由前两者推出，未直接观测。

---

## 0. 这份清单怎么读

### 0.1 分级

**分级只按一件事**：不做会怎样。

| 级 | 含义 | 判据 |
|---|---|---|
| **S0** | 不做就继续带着已知风险跑 | 已在真机出过事故，或需要一个只有人能给的决定 |
| **S1** | 不做会在下一次同类缺陷上再花一遍时间 | 结构上「看起来被守住了，实际上没有东西会红」 |
| **S2** | 不做只是让下一个人多读一会儿 | 重复、命名、注释、死代码 |
| **V** | 待核实 —— **不要用文档猜** | 需要一个外部事实（账单 / 厂商 / 真机） |

原先三份来源用的是两套判据：`P0/P1/P2`（能不能让产品闭环成立）与 `S0/S1/S2`（不做会怎样）。
两套**近乎对应**（P0≈S0、P1≈S1、P2≈V），合并时按上表归一。

### 0.2 编号

**编号是稳定 id，不是分级。** 三条来源的编号**一律保留原样、不改写、不复用** ——
因为全仓有 **58 个文件**引用 `P0-x`/`P1-x`/`P2-x`（`P1-2` 被引 48 次、`P0-1` 33 次），
重编号会打断它们。

**唯一一处消歧**：iOS 与 backend 的 `S0-x`/`S1-x`/`S2-x` **编号完全撞车**
（`S1-1` 在 iOS 是「`pcmBuffer` 不变量」，在 backend 是「`materials` 无租约」）。
所以两套 S 号**加仓前缀**：

| 来源 | 编号写法 |
|---|---|
| 产品与缺陷轴（原 77_） | `F1` / `P0-10` / `P1-17` / `P2-4` —— 原样，无前缀（本来就全局唯一） |
| iOS 架构轴（原 iOS `80_/05_`） | **`iOS-S0-1`** / **`iOS-S1-9`** / **`iOS-S2-11`** |
| backend 架构轴（原 backend `104_/05_`） | **`BE-S0-1`** / **`BE-S1-1`** / **`BE-S2-6`** |

**引用时一律带前缀。** 写「见 `S1-9`」是错的 —— 它有两个所指。

### 0.3 用法规则

1. 每修一条，在对应行标 ✅ 并补上提交哈希与实现说明链接。
2. **一次只推进一条**，跨仓串行（后端与 iOS 不同时改）。
3. **新增问题直接进表，不另开文档。** —— 这条在只有一张表时才真的可执行。
4. **状态只在子文件里改，本文 §2 是视图。**
   ⚠️ **两个仓的 `docs/` 已于 2026-09-25 整体删除**（iOS `80c8209`、backend `e2c61f7`），
   连同 `01`–`04` 架构分析、`70_tts_wss_refactor/`、`103_` 与各实现说明。
   所以三个子文件现在是这些条目**唯一的载体**，不再是「收敛件」；
   子文件里指向原 `01`–`04` 的行号引用**已失效**。
5. 表变长不是坏事 —— **变长且没人动才是。**

---

## 1. 接下来做什么

三条来源各自都有「如果只做三件事」。合并后的排序：

### 1.1 已决，等动手

| 条目 | 内容 | 卡在哪 |
|---|---|---|
| **`iOS-S0-1` + `BE-S0-1`** | **Stage 3：二进制音频帧带轮次归属（`turn_ref`）** —— 同一件事的两侧，**已决：排期，作为下一项主任务**。契约见 `meta docs/30_技术方案/100_`（原草案 `ios docs/70_tts_wss_refactor/25_` §3 **已随 docs/ 删除**）。**四个问题已于 2026-09-25 按答复里的推荐拍板**（编号域=会话级独立分配器 / 字段可选 / 客户端先发后网关、回滚=网关停发 / 冻结产物落 `infra/schemas/transport/`） | 后端 + iOS 同时改，**跨仓必须串行**；动手前先在 infra 落冻结产物 |

### 1.2 不需要新决定、可以立刻做

| 条目 | 内容 | 为什么排前面 |
|---|---|---|
| **`iOS-S1-2` + `iOS-S1-3`** | 11 份漂移的等待助手收敛成一份；超时值的理由只有一份副本，而另一个文件还有 65 处 1 秒 | iOS 侧**最具体**的发现；两条合起来是一次改动 |
| **`BE-S1-1`** | `materials` 的 refine 没有租约，`processing` 是没有出口的状态 | 用户可见的失败模式 + 同仓有现成模板（`session_jobs`）+ 不需要新设计 |
| **`BE-S1-2` + `BE-S1-3`** | `httpjson.Error` 补 4 个分支的测试；给手写解码器 `DecodeAITTSAudio` 写 fuzz（全仓 0 个 fuzz） | 补的是这个仓完全空着的两类覆盖（错误路径 / 输入边界） |
| **`iOS-S1-10` 的结构面** | `AVAudioSession` 两个实现点：`LiveAudioEngine` 走 `AudioSessionManaging`，`DailyReadAudioPlayer` 绕过它 | 2026-09-24 真机事故的残余；局部修复已做，结构面仍开着 |

### 1.3 需要人给决定

| 条目 | 内容 | 谁 |
|---|---|---|
| `BE-S0-2` / `BE-S0-3` | 迁移机制的归属；26 个迁移里 22 个没有 down —— 补 down 或正式声明「只前滚」 | Tango / 运维 |
| `BE-S0-4` | `topic_cards` 加唯一键（需先确认历史数据无重复） | Tango（数据确认） |
| `BE-S0-5` | `voicegateway` 的拆分目标形状 | Tango / 架构 |
| `BE-S0-6` | 网关**要不要**「携 `session_id` 恢复会话」的能力 —— 要的话，三样前置（可携带 id 的帧 / 会话注册表 / 活上下文持久化）**一样都不存在** | **产品决定**（要不要这个能力）；`P1-15` ✅ 已指明「这是一张独立的后端票」 |
| `iOS-S0-5` | 远端 `main` 的分支保护要求 PR + 3 项状态检查，而 `AGENTS.md` 写着「develop on `main`, no PRs」 | 仓库管理员（执行不了） |
| `P0-13` | 说的房间的入口应是会话列表，不是工作台里的一个模块 | **产品形态变更**，动手前要先定两件事 |
| `P0-15` A 半 | 空会话该不该进回顾管线 / 要不要第一次真正说话时才建会话 | **产品决定** |
| `P2-1` / `P2-2` / `P2-3` / `P2-4` | 模型版本号、成本口径、商务三项、prod TTS SKU 授权 | 外部（厂商 / 账单） |

### 1.4 只能靠真机

| 条目 | 内容 |
|---|---|
| **`iOS-S0-3`** | 真机验证 barge-in 与断线重连（需一次 >5 秒的回复）。**不是「补测」** —— 本仓的测试层级结构上测不到那类问题，只能排时间 |
| `P0-2` | AEC 从未被验证（引擎级开关已接上，效果未验） |
| `P0-11` / `P0-12` / `P2-5` | 三条根因未定性的用户报告，都需要那一场的 iOS 真机日志 |
| `P1-6` | `abort` 后 ASR 是否串味未验 |

**明确不做（现在）**：把端到端 duplex 换成分解流水线 —— 基线不存在，见 [`问题总清单-产品与缺陷.md`](./问题总清单-产品与缺陷.md) 的「执行计划」。

---

## 2. 按阶段索引（未关闭条目）

**已关闭的不进索引** —— 它们的提交哈希与红验证记录在各子文件里（`F1`–`F21`、`P0-1`、`P1-1`–`P1-5`、`P1-7`、`P1-9`–`P1-21`、`P1-23`、`P1-25`、`iOS-S1-1`、`iOS-S1-9`、`iOS-S2-9`）。

### S0 — 需要人拍板 / 已出真机事故

| 编号 | 仓 | 一句话 | 状态 |
|---|---|---|---|
| `iOS-S0-1` | ios | Stage 3：二进制帧带轮次归属 | **已决：下一项主任务** |
| `BE-S0-1` | backend | 二进制帧带 `turn_id`（**与 `iOS-S0-1` 是同一条**） | 跨仓，契约待定 |
| `iOS-S0-2` | ios | D5 解码失败策略 | **已决：选项 2**，未落地 |
| `iOS-S0-3` | ios | 真机验证 barge-in 与断线重连 | 设备与时间 |
| `iOS-S0-4` | ios | 两个设计文档分支的处置 | **已决：不并进 main**，未落地 |
| `iOS-S0-5` | ios | 远端 `main` 分支保护 vs `AGENTS.md` | 需仓库管理员 |
| `BE-S0-2` | backend | 迁移机制的归属（Go 迁移工具 / 声明「只前滚」） | 需决定 |
| `BE-S0-3` | backend | 26 个迁移里 22 个没有 down | 需决定 |
| `BE-S0-4` | backend | `topic_cards` 加唯一键 | 需数据确认 |
| `BE-S0-5` | backend | `voicegateway` 的拆分目标形状 | 需架构决定 |
| `BE-S0-6` | backend | 网关不能恢复会话（`P1-15` 指认的「独立后端票」） | 需产品决定「要不要」 |
| `P0-2` | ios | AEC 从未被验证 | 真机 |
| `P0-9` | backend | 上游 duplex 静默重开 → 会话上下文丢失（约 10% 的轮） | 根因未定性 |
| `P0-10` | ios+backend | 「昨天那场今天进不去也聊不了」 | ①②③ 已修，第 3 片（进房间）未做 |
| `P0-11` | ios | 助手音频要等点击才播放、且放的是上一轮内容 | 根因未定性 |
| `P0-12` | ios | TTS 播放中点「开始说话」→ 卡在「识别中」，无超时 | 根因未定性 |
| `P0-13` | ios | 房间入口应是会话列表，不是工作台模块 | **产品形态变更**，需先定 |
| `P0-14` | ios | 点「结束练习」后音频还在播，有几声「沙沙」 | 未修，**故意的**（顺序改动是推测） |
| `P0-15` | backend+产品 | 回顾页显示兜底内容而用户看不出来 | A 半需产品决定 / B 半未做 |

### S1 — 防线

| 编号 | 仓 | 一句话 | 状态 |
|---|---|---|---|
| `iOS-S1-2` | ios | 11 份漂移的等待助手 | 可立刻做 |
| `iOS-S1-3` | ios | 超时值的理由只有一份副本，另一文件还有 65 处 1 秒 | 可立刻做 |
| `iOS-S1-4` | ios | 文件 1735 行的切缝选择（原推荐改法**不成立**） | 设计决定 |
| `iOS-S1-5` | ios | 无端到端测试（`Integration/` 只有 51 行） | 成本最高 |
| `iOS-S1-6` | ios | DI 是「可注入」而非「必须注入」（10 处 `?? Container.shared`） | |
| `iOS-S1-7` | ios | D8：音频路径每帧两次 actor 跳转 | **先量基线** |
| `iOS-S1-8` | ios | `FWTryCatch` 是崩溃面的唯一防线，5 个调用点 | 查真机日志 |
| `iOS-S1-10` | ios | `AVAudioSession` 两个实现点，其中一个绕过 owner | 局部已修，**结构面开着** |
| `BE-S1-1` | backend | `materials` 的 refine 没有租约 | 可立刻做 |
| `BE-S1-2` | backend | `httpjson.Error` 零测试（4 个分支） | 可立刻做 |
| `BE-S1-3` | backend | 手写解码器没有 fuzz（全仓 0 个） | 可立刻做 |
| `BE-S1-4` | backend | 控制帧分派线性扫描，同一帧最多解码 8 次 | |
| `BE-S1-5` | backend | `ai.audio.chunk` 常量无生产者、无注释 | |
| `BE-S1-6` | backend | 音频热路径逐帧 `Debug` 日志，成本无法量化 | 先加 benchmark |
| `BE-S1-7` | backend | `providerErrorStrategies` 的 key 与 handler 对应只靠读 | |
| `BE-S1-8` | backend | `config.Config` 夹具复制到 23 个文件（约 56 处） | |
| `BE-S1-9` | backend | 原文称「`golangci-lint` 未安装」 | ⚠️ **已过期，应关闭** —— 见下 |
| `P1-6` | backend | `abort` 后 ASR 是否串味未验 | 真机 |
| `P1-8` | ios | badge 的时钟与音频的时钟独立（4s 窗口 vs 播放长度） | 待验 |
| `P1-22` | ios | 没有任何测试构造过真实 socket（前半未做） | 需独立基建 |
| `P1-24` | ios | 离开房间再回来内容没了 | 第 3 片未做 |

> **`BE-S1-9` 的更正（2026-09-25 实测）**：原文写「`golangci-lint` 未安装 → `depguard` 那条纪律在本机从未执行过」。
> **这是错的。** `~/go/bin/golangci-lint` 存在（`v2.13.2`，2026-09-20 装），
> 而 `scripts/dev-check.sh:7` 逐字 `export PATH="$(go env GOPATH)/bin:$PATH"` ——
> 所以每次门禁 `depguard` **都真的跑了**。真实情况只是「不在默认 PATH 上」。
> 这条已躺了两天没人发现，是本清单**需要回填与核对**的直接证据。

### S2 — 卫生

| 编号 | 仓 | 一句话 |
|---|---|---|
| `iOS-S2-1` | ios | 同名替身跨文件重复：`StubCorpusClient` ×3、`StubDailyReadAPIClient` ×3 |
| `iOS-S2-2` | ios | `AppBootstrapMiddleware.swift`（836 行）定义 **4 个**无关 middleware |
| `iOS-S2-3` | ios | `appCrossCuttingReducer` 是没被命名的「第二套 reducer」 |
| `iOS-S2-4` | ios | 手写的去重/闸门有 5 个，而框架提供 `.debounce`（全仓用 0 次） |
| `iOS-S2-5` | ios | 三种隔离策略并存（17 actor / 5 `@MainActor` / 11 锁） |
| `iOS-S2-6` | ios | 7 个 `.shared` 进程单例，没有逐个论证为什么必须是单例 |
| `iOS-S2-7` | ios | Moya 钉在个人 fork 的 `master` 分支，不是 tag/revision |
| `iOS-S2-8` | ios | `Modules/` 是空目录（只有 `.gitkeep`） |
| `iOS-S2-10` | ios | `sendInterrupt()` 吞掉 `try?` |
| `iOS-S2-11` | ios | `handshake` 是 iOS 侧死面（`Shared/` 从不构造它） |
| `iOS-S2-12` | ios | 丢弃的**计数**没有上报 |
| `BE-S2-1` | backend | `AGENTS.md:22` 把 `internal/corpus/` 写成 `internal/corpuss/` |
| `BE-S2-2` | backend | `handler.go:776-778` 有一段描述**不存在的函数**的孤儿注释 |
| `BE-S2-3` | backend | `uplink_constants.go` 的注释说「两份 uplink 路径都用这个常量」，实际有第二份 |
| `BE-S2-4` | backend | `httpserver` 的 `discovery` 手写了一份**部分**端点清单 |
| `BE-S2-5` | backend | `/metrics` 是 7 个包手写文本的拼接，无 registry、无重名检测 |
| `BE-S2-6` | backend | 7 个指标发射器里只有 2 个对 label 集合排序 → `/metrics` 输出不可复现 |
| `BE-S2-7` | backend | `test/` 是空目录（只有 `.gitkeep`） |
| `BE-S2-8` | backend | 51 个环境变量没有一份清单（唯一来源是两个 `Config` 结构体） |

### V — 待核实（不要用文档猜）

| 编号 | 仓 | 一句话 |
|---|---|---|
| `P2-1` | backend | 模型版本 `1.2.6.0`（我们配置）vs 官方文档固定值 `1.2.6.1` |
| `P2-2` | backend | 成本口径：官方计费文档**自身矛盾**（差 2.67 倍）—— 以账单为准 |
| `P2-3` | — | `meta #12` 商务三项（免费额度 / 数据不训练 / 并发配额）未闭环 |
| `P2-4` | backend | Prod 侧 TTS SKU 授权（曾有 403 / `code=55000000`） |
| `P2-5` | ios | 真机上「回复」看起来抢在「用户那句话」之前 —— 待量化 |

---

## 3. 三条轴（按章节拆分的子文件）

| 文件 | 内容 | 编号 |
|---|---|---|
| [`问题总清单-产品与缺陷.md`](./问题总清单-产品与缺陷.md) | 前提 · 已修复 `F1`–`F21` · 待修复 `P0`/`P1`/`P2` · AEC 怎么做 · 执行计划 · PRD 层问题 | `F…` / `P0-x` / `P1-x` / `P2-x` |
| [`问题总清单-iOS架构.md`](./问题总清单-iOS架构.md) | `S0`/`S1`/`S2` · 反过来：不要改 · 如果只做三件事 · 本轮已关闭 · 与其它文档的关系 | `iOS-S…` |
| [`问题总清单-backend架构.md`](./问题总清单-backend架构.md) | `S0`/`S1`/`S2` · 反过来：不要改 · 如果只做三件事 · 与 `103_` 的关系 · 与 iOS 侧的对应 | `BE-S…` |

**两个架构轴文件原是 `01`–`04` 四章的收敛。**
⚠️ 那四章连同两个仓的整个 `docs/` 已于 2026-09-25 删除，**收敛件成了孤本**；
子文件里形如「`01`–`04` §x.y」的行号引用**不再可解析**，只能当作历史标注读。

---

## 4. 关联文档

⚠️ **2026-09-25 起，iOS 与 backend 的 `docs/` 已不存在。** 下表只列仍然存在的东西。

| 想知道 | 去哪 |
|---|---|
| 契约与债分析 | `meta/docs/30_技术方案/`（`83_`/`84_` 契约草案、`99_` 债四分类、`100_` Stage 3 答复） |
| 环境怎么起 | `meta/docs/40_研发流程与协作/82_FluentWork环境启动总览.md` |
| 各仓怎么构建、门禁是什么、有什么已知漂移 | 各仓根目录的 `README.md` / `AGENTS.md` / `CLAUDE.md`（2026-09-25 重建） |
| 共享 agent 治理与模板 | `meta/agents/`（`shared/` 六份策略 + `templates/`） |
| 共享 schema 真源 | `fluentwork-infra/schemas/`（`transport/`、`events/`） |
| ~~单条缺陷的根因与红输出~~ | **已随 `docs/` 删除，只在本清单条目文字里留有摘要** |
| ~~iOS / backend 的分层、DI、语音链路分析~~ | **已随 `docs/` 删除** |
| ~~真机测试手册~~ | **已随 `docs/` 删除**（原 `fluentwork-ios/docs/62_device_test_manual.md`） |
| 下一步会话从哪读起 | 本文 §1 |
