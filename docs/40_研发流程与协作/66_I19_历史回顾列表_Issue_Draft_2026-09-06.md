# I19 历史回顾列表（C4） — GitHub Issue 草稿

**仓库**：`FluentWork/fluentwork-ios`
**优先级**：P2（W4 第 5 日，B24 落地后启动）
**估时**：0.5 dev-day
**关联文档**：
- `docs/30_技术方案/32_FluentWork-iOS App端技术设计文档.md` §十
- `docs/30_技术方案/48_FluentWork_V2_REST接口契约冻结_2026-09-06.md` §1.3.2
- `docs/40_研发流程与协作/60_iOS_W3_W4_代码层启动包_2026-09-06.md`
- 父 Issue：B24（历史回顾 API，blocked → B24 CLOSED 后启动）

---

## 🎯 目标

实现 PRD §C4 历史回顾列表入口：
- 工作台新增「历史回顾」入口
- 列表展示用户过去 30 天的会话（cursor 分页）
- 点击进入会话详情（复用 I16 SessionReviewView）

---

## 🚧 阻塞条件

- **B24（历史回顾 API）CLOSED**—— `GET /api/v1/sessions` 支持 cursor 分页 + 时间筛选已实施

---

## 📋 实施步骤

### 1. 数据模型（`Sources/History/HistoryModels.swift`）

```swift
struct SessionListItem: Codable, Identifiable {
    let sessionID: String
    let materialIDs: [String]
    let reviewStatus: String  // "pending" / "ready" / "failed"
    let startedAt: Date
    let endedAt: Date?
    
    var id: String { sessionID }
}

struct SessionListPage: Codable {
    let items: [SessionListItem]
    let nextCursor: String?
}
```

### 2. ViewModel（`Sources/History/SessionListViewModel.swift`）

```swift
@MainActor
final class SessionListViewModel: ObservableObject {
    @Published var items: [SessionListItem] = []
    @Published var nextCursor: String?
    @Published var isLoading = false
    @Published var error: APIError?
    
    private let api: SessionAPIClient
    
    func loadInitial() async {
        items = []
        nextCursor = nil
        await loadMore()
    }
    
    func loadMore() async {
        guard !isLoading else { return }
        isLoading = true
        defer { isLoading = false }
        do {
            let page = try await api.listSessions(cursor: nextCursor)
            items.append(contentsOf: page.items)
            nextCursor = page.nextCursor
        } catch {
            self.error = error as? APIError
        }
    }
}
```

### 3. View（`Sources/History/SessionListView.swift`）

```swift
struct SessionListView: View {
    @StateObject var viewModel = SessionListViewModel()
    
    var body: some View {
        List {
            ForEach(viewModel.items) { item in
                NavigationLink {
                    SessionReviewView(sessionID: item.sessionID)
                } label: {
                    SessionRow(item: item)
                }
                .onAppear {
                    if item == viewModel.items.last {
                        Task { await viewModel.loadMore() }
                    }
                }
            }
            
            if viewModel.isLoading {
                ProgressView()
                    .frame(maxWidth: .infinity)
            }
        }
        .navigationTitle("历史回顾")
        .task { await viewModel.loadInitial() }
    }
}

struct SessionRow: View {
    let item: SessionListItem
    var body: some View {
        HStack {
            VStack(alignment: .leading) {
                Text(item.startedAt.formatted(date: .abbreviated, time: .shortened))
                    .font(.headline)
                Text("素材 \(item.materialIDs.count) 个")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
            Spacer()
            StatusBadge(status: item.reviewStatus)
        }
    }
}

struct StatusBadge: View {
    let status: String
    var color: Color {
        switch status {
        case "ready": return .green
        case "failed": return .red
        default: return .orange
        }
    }
    var body: some View {
        Text(status).font(.caption)
            .padding(.horizontal, 8).padding(.vertical, 4)
            .background(color.opacity(0.15))
            .foregroundStyle(color)
            .clipShape(Capsule())
    }
}
```

### 4. 工作台入口（`Sources/Home/HomeDashboardView.swift` 增强）

```swift
// 在 HomeDashboardView 添加入口卡片
NavigationLink {
    SessionListView()
} label: {
    HomeEntryCard(
        icon: "clock.arrow.circlepath",
        title: "历史回顾",
        subtitle: "查看过去的练习"
    )
}
```

---

## ✅ 测试矩阵

| ID | 场景 | 预期 |
|---|---|---|
| T-I19-1 | 首页 → 历史回顾 → 显示列表 | 最多 20 条（默认 size） |
| T-I19-2 | 滚动到底 → 自动加载下一页 | cursor 衔接正确 |
| T-I19-3 | 点击 session → 跳转详情 | 复用 I16 SessionReviewView |
| T-I19-4 | 空列表（无历史） | 显示「还没有练习，去开始第一次」+ 跳转入口 |
| T-I19-5 | 网络异常 | 显示「加载失败，重试」按钮 |
| T-I19-6 | review_status=pending | 黄色 badge「生成中」+ 详情页提示 |
| T-I19-7 | review_status=failed | 红色 badge「失败」+ 详情页可重试 |
| T-I19-8 | 时间筛选（since=） | V1.5，本期不实现 |
| T-I19-9 | session 长时区间（30 天前） | 仅显示 since 之后（默认 30d） |
| T-I19-10 | VoiceOver | 列表项可朗读 + status badge 可朗读 |

---

## 📐 估时拆分

| 步骤 | 估时 |
|---|---|
| 1. 数据模型 + API Client | 0.1 dev-day |
| 2. SessionListViewModel + 分页 | 0.2 dev-day |
| 3. SessionListView + StatusBadge | 0.2 dev-day |
| **合计** | **0.5 dev-day** |

---

## 🎯 DoD

- [ ] `Sources/History/` 整个目录新建
- [ ] `SessionAPIClient.listSessions` 实现（cursor 分页）
- [ ] `HomeDashboardView` 集成入口
- [ ] Snapshot 测试（4 种状态：加载中 / 有数据 / 空 / 失败）
- [ ] 端到端冒烟：staging backend + 真实设备走通
- [ ] i18n + Dark Mode + VoiceOver 通过
- [ ] PR 通过 OpenCodeReview + 1 名 iOS maintainer review
- [ ] Issue description 链接本文 + 60_ 启动包 + 48 §1.3.2 + 32 §十
- [ ] 完成后状态写到 53_ §Layer 2 表格（CLOSED ✅）

---

## 🔗 关联 Issue

- 上游（阻塞）：B24（历史回顾 API）
- 依赖：I16 SessionReviewView（已 live）
- 下游：iOS 历史回顾详情 V2.0 增强（V1.5 启动）
- 平行：I14 / I15 / I16 / I17 / I18（同 W4 批次）
