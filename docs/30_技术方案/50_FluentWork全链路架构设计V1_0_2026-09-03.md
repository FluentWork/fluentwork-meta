# FluentWork 全链路架构设计文档

> 文档版本：v1.0  
> 创建日期：2026-09-03  
> 适用范围：fluentwork-backend + fluentwork-ios 全栈架构

---

## 一、系统架构概览

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         iOS Client (SwiftUI)                        │
│                                                                     │
│  UI Layer          SpeakingRoomView                                 │
│                    ├── HoldToSpeakButton (手动 VAD)                │
│                    └── VADStatusIndicator (自动 VAD)               │
│                           ↓                                          │
│  State Layer       SpeakingRoomFeature (TGReduxKit)                │
│                    ├── SpeechSessionMachine (纯状态机)              │
│                    └── SpeechSessionMiddleware (副作用解释器)       │
│                           ↓                                          │
│  Service Layer      DefaultSpeechSessionClient                      │
│                    ├── SocketTransport (WSS 连接)                   │
│                    └── AudioEngine (音频采集/播放)                   │
│                                                                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ WSS (PCM 音频流 + JSON 控制帧)
                               │ TLS 加密
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     Backend (Go)                                     │
│                                                                     │
│  Gateway Layer      voice-gateway (端口 8081)                        │
│                    ├── Handler         (WSS 握手、帧分发、错误处理)  │
│                    ├── sessionRuntime  (会话运行时状态)              │
│                    └── logWarn         (WARN 去重)                   │
│                           ↓                                          │
│  Provider Layer     VoiceProvider 接口                               │
│                    ├── MockVoiceProvider    (测试用)                  │
│                    ├── DevEchoVoiceProvider (本地开发)               │
│                    └── VolcDuplexProvider  (生产火山引擎)            │
│                           ↓                                          │
│  Vendor Layer      voicepoc/volc_duplex.go                          │
│                    ├── DuplexSession     (火山 SDK 封装)             │
│                    └── TurnResult        (turn 结果 + Outcome)       │
│                                                                     │
│  App Layer         app-server (端口 8080)                           │
│                    ├── Session Lifecycle (Activate/End)              │
│                    ├── Utterance 持久化                              │
│                    └── Background Worker (review job)                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 组件 | 技术选型 | 版本 |
|------|----------|------|
| iOS UI | SwiftUI | iOS 17+ |
| iOS 状态管理 | TGReduxKit (Redux 风格) | 最新 |
| iOS 网络 | Moya + URLSessionWebSocketTask | 最新 |
| iOS 音频 | AVAudioEngine | 系统框架 |
| Backend HTTP | Gin | Go |
| Backend WSS | github.com/coder/websocket | 最新 |
| 语音供应商 | Volcengine Seeduplex | v1.2.6.0 |
| 数据库 | MySQL + in-memory fallback | - |
| 日志 | log/slog (JSON structured) | Go 标准库 |

---

## 二、关键模块设计

### 2.1 状态机设计 (iOS)

```
                        ┌─────────────────────────────────────────────┐
                        │           Session Lifecycle                  │
                        └─────────────────────────────────────────────┘

  ┌──────────┐    ┌─────────────┐         ┌──────────────────┐
  │   idle   │───→│ connecting  │────────→│    waitingUser    │
  └──────────┘    └─────────────┘         └────────┬─────────┘
                     │   ↑                              │
                     │   └──────────────────────────────┘
                     │        (session.start sent)
                     │
  ┌─────────────────▼──────────────────────────┐
  │              .failed("turn_timeout")       │
  │              .failed("socket_error")       │────→ .ended
  └────────────────────────────────────────────┘

                        ┌─────────────────────────────────────────────┐
                        │           Turn Lifecycle (Loop)              │
                        └─────────────────────────────────────────────┘

  ┌──────────────┐  user speaks  ┌────────────┐  VAD end  ┌──────────┐
  │ waitingUser  │──────────────→│ recording  │───────────→│processing│
  └──────────────┘               └────────────┘            └────┬─────┘
                                                               │
  ┌──────────────────────────────────────────────────────────────┘
  │                        ai.turn.end
  ↓
  ┌──────────────┐  user speaks  ┌────────────┐  VAD end  ┌──────────┐
  │ aiSpeaking   │──────────────→│ waitingUser│───────────→│processing│
  └──────────────┘               └────────────┘            └──────────┘
        ↑                                                         │
        └─────────────────────────────────────────────────────────┘
                        (下一轮循环)
```

**关键设计原则：**
- **纯函数 reduce**：`SpeechSessionMachine.reduce(&state, event)` 无副作用
- **幂等转换**：同一 (phase, event) 永远产生相同结果
- **超时隔离**：每个 phase 有独立的超时计时器

### 2.2 Provider 架构

```
                    VoiceProvider (interface)
                           │
            ┌──────────────┼──────────────┐
            │              │              │
    MockVoiceProvider  DevEchoVoiceProvider  VolcDuplexProvider
            │              │              │
     (返回空文本)    (本地开发 Echo)   (生产火山引擎)
                       │
                       │ T2: 支持 PCM fixture 流式返回
```

**Provider 接口契约：**

```go
type VoiceProvider interface {
    Open(ctx context.Context, ticket ConsumedTicket) (VoiceProviderSession, error)
}

type VoiceProviderSession interface {
    Start(ctx context.Context, start voiceproto.SessionStart) ([]ProviderOutbound, error)
    HandleClientControl(ctx context.Context, frameType string, raw []byte) ([]ProviderOutbound, error)
    HandleClientAudio(ctx context.Context, payload []byte) ([]ProviderOutbound, error)
    SnapshotUtterances() []EndUtterance
    Close(ctx context.Context) error
}
```

### 2.3 标识符体系设计

**目标：session_id / turn_id / log_id 贯穿全链路，支持端到端回放。**

```
iOS 发起 session
    ↓ POST /sessions
app-server 生成 session_id
    ↓ 返回 wssURL + session_id + ticket
iOS 连接 voice-gateway (WSS)
    ↓ auth frame 携带 ticket
voice-gateway 消费 ticket → ConsumedTicket { SessionID, UserID }
    ↓
iOS 发送 user.speech.end(turn_id="turn-1")
    ↓
VolcDuplexProvider.WaitTurnResult()
    ↓ 火山引擎返回 transcript + LogID
    ↓
voice-gateway → iOS
  ├─ client.asr.transcription(turn_id, text)
  ├─ ai.audio.chunk(pcm)
  └─ ai.turn.end(turn_id, outcome, log_id)  ← 全链路 trace anchor
```

**命名规范：**
- `session_id`：UUID v4，app-server 生成，全局唯一
- `turn_id`：`"turn-{N}"`，N 从 1 开始，iOS 和后端使用同一计数器
- `log_id`：火山引擎 `X-Tt-Logid`，单次 DuplexSession 生命周期内不变

### 2.4 音频格式设计

```
┌────────────────────────────────────────────────────┐
│              音频格式约定                           │
├────────────────────────────────────────────────────┤
│ 采样率      │ 16,000 Hz                           │
│ 通道数      │ 1 (mono)                            │
│ 位深        │ 16-bit signed integer (PCM16)        │
│ 交错方式    │ interleaved (AVAudioEngine 默认)     │
│ 帧长        │ 20ms = 640 bytes                    │
│ 编码        │ 原始 PCM (WSS 二进制帧)              │
│ B1 降级     │ 设备不支持 16kHz → 自动重采样        │
└────────────────────────────────────────────────────┘
```

**iOS 音频管道：**
```
AVAudioEngine.inputNode (设备原生采样率)
    ↓
AVAudioConverter → 16kHz mono PCM16 (AVAudioEngine tap)
    ↓
WSS 二进制帧 → voice-gateway
```

---

## 三、难点分析

### 3.1 状态机一致性

**难点描述：** iOS 端状态机（phase）和后端 session 状态需要保持同步。当 iOS 在 `.processing` 状态时，后端可能在等待 Volc 返回；若 Volc 超时，iOS 和后端的状态必须一致地切换到错误状态。

**当前设计：**
- 后端：TurnOutcome 明确告知超时，iOS 通过 `ai.turn.end` 的 `outcome=timeout` 感知
- iOS：B15 70s 客户端兜底 timer 作为最后防线

**优化方向：** 未来可引入 WSS 心跳帧携带 server-side phase，客户端据此对齐状态。

### 3.2 VAD 跨设备一致性

**难点描述：** `speechThreshold=0.015` 和 `silenceHold=1500ms` 在不同设备、不同噪音环境下表现差异显著。

**当前设计：**
- MVP：手动按住说话作为主路径，消除 VAD 依赖
- 自动 VAD：作为可选降级路径

**优化方向：** 未来可引入设备校准流程（首次启动引导用户说一句话建立 baseline 能量）。

### 3.3 PCM fixture 格式验证

**难点描述：** DevEchoProvider 支持 PCM fixture 但没有格式校验机制。错误的 PCM 文件（采样率不对、非 PCM 格式）会导致不可预期的行为。

**当前设计：** `DevEchoFixtureGenerator` 硬编码 16kHz mono PCM16LE，无外部校验。

**优化方向：** 在 `FixturePCMLoader` 中增加格式校验：
```go
func LoadWAVPCM16LE(path string) ([]byte, int, error) {
    // 校验：文件大小、RIFF/WAVE header、必要字段存在
    // 返回：(pcm数据, sampleRate, error)
}
```

### 3.4 全链路时间对齐

**难点描述：** Go 端 `logx.Begin/End` 和 iOS 端 `SpeechSessionTimingsRecorder.mark` 各自记录时间戳，基准时钟不同（Go 用 `time.Now()`，iOS 用 `ContinuousClock.now`），无法精确对齐同一事件。

**当前设计：** 两端都记录 wall-clock 毫秒数，通过 `timing_*` 事件名的约定关联。

**优化方向：** 在 WSS 控制帧中携带 server-side timestamp（如 `ai.turn.end` 增加 `server_ts_ms`），iOS 收到后用本地时钟计算 delta，消除时钟漂移。

---

## 四、安全设计

### 4.1 认证与授权

```
1. iOS 登录 → app-server → 返回 JWT access_token
2. iOS POST /sessions → Authorization: Bearer <token>
   → 返回 { wssURL, session_id, ticket }
3. ticket 为一次性令牌，WSS 认证后消耗，不可复用
4. WSS 断开后 ticket 失效，需重新走步骤 2
```

### 4.2 敏感数据

| 数据 | 保护方式 |
|------|----------|
| Volc API Key | 环境变量注入，不进入代码仓库 |
| User JWT | HTTPS 传输，内存存储不过磁盘 |
| 用户录音 | 不持久化，仅在 WSS session 生命周期内流式处理 |
| 录音转写文本 | 与 session 一起存入 MySQL，用户可删除 |

---

## 五、可观测性设计

### 5.1 日志结构

```json
{
  "level": "INFO",
  "ts": 1725331200000,
  "event": "voice.duplex.collect_turn",
  "module": "voicepoc.duplex",
  "provider": "volc-duplex",
  "session_id": "sess_abc123",
  "turn_id": "turn-1",
  "log_id": "20260903120000abc",
  "duration_ms": 2340.5,
  "outcome": "ok",
  "transcript_len": 45,
  "assistant_text_len": 128
}
```

### 5.2 iOS 事件追踪

```swift
tracker.track(event: "turn_timeout_fired", properties: [
    "timeout_sec": "70",
    "turn_id": "turn-3",
    "session_id": "sess_abc123"
])

tracker.track(event: "transport_rx", properties: [
    "frame_type": "ai.turn.end",
    "outcome": "timeout",
    "log_id": "20260903120000abc"
])
```

### 5.3 关键 SLO

| 指标 | 目标 |
|------|------|
| Turn 完成率（60s 内收到 ai.turn.end） | ≥ 95% |
| P99 Turn 延迟（user.speech.end → ai.turn.end） | ≤ 30s |
| iOS 客户端 70s 超时触发率 | ≤ 5%（正常情况下不应触发） |
| badge 命中率（有语料场景） | ≥ 70% |

---

## 六、扩展性设计

### 6.1 Provider 扩展

新增 Provider 只需实现 `VoiceProviderSession` 接口：

```go
type VoiceProviderSession interface {
    Start(ctx context.Context, start voiceproto.SessionStart) ([]ProviderOutbound, error)
    HandleClientControl(ctx context.Context, frameType string, raw []byte) ([]ProviderOutbound, error)
    HandleClientAudio(ctx context.Context, payload []byte) ([]ProviderOutbound, error)
    SnapshotUtterances() []EndUtterance
    Close(ctx context.Context) error
}
```

工厂模式注册：
```go
func NewVoiceProvider(cfg Config) VoiceProvider {
    switch cfg.Provider {
    case "openai-realtime": return NewOpenAIRealtimeProvider(...)
    case "azure-speech":   return NewAzureSpeechProvider(...)
    default:                return MockVoiceProvider{}
    }
}
```

### 6.2 FeatureFlag 扩展

iOS FeatureFlag 支持远程配置，可动态切换：
- `voice_input_mode`: manual / autoVAD
- `turn_timeout_seconds`: 70（可远程调参）
- `vad_speech_threshold`: 0.015（可远程调参）

---

## 七、文件结构索引

```
fluentwork-backend/
├── cmd/
│   ├── app-server/          # HTTP API server
│   └── voice-gateway/       # WSS voice gateway
├── internal/
│   ├── voicegateway/        # WSS handler + providers
│   │   ├── handler.go       # ⭐ 核心：帧分发、sessionRuntime
│   │   ├── provider.go      # Provider 接口
│   │   ├── provider_dev_echo.go  # 本地开发 Provider
│   │   ├── provider_volc_duplex.go  # 生产 Provider
│   │   ├── badge_emitter.go # B12 语料命中检测
│   │   └── voiceproto/frames.go  # 帧类型定义
│   ├── voicepoc/            # 火山引擎 POC 封装
│   │   └── volc_duplex.go  # ⭐ TurnResult + TurnOutcome
│   ├── session/             # Session 生命周期
│   └── apierr/              # 统一错误格式
├── pkg/
│   └── logx/               # 结构化日志
└── docs/
    └── i20-fix-plan.md      # I20 P0/P1 修复计划

fluentwork-ios/
├── Shared/
│   ├── FluentWorkCore/
│   │   ├── SpeechSession/
│   │   │   ├── SpeechSessionMachine.swift   # ⭐ 纯状态机
│   │   │   └── SpeechSessionTimingsRecorder.swift  # 时间线记录
│   │   ├── Services/
│   │   │   ├── LiveAudioEngine.swift        # ⭐ 音频采集+VAD
│   │   │   └── DefaultSpeechSessionClient.swift
│   │   └── Architecture/
│   │       ├── Middleware/
│   │       │   └── SpeechSessionMiddleware.swift  # ⭐ 副作用解释器+B15 超时
│   │       └── Features/
│   │           └── SpeakingRoomFeature.swift
│   ├── FluentWorkNetworking/
│   │   └── Socket/
│   │       ├── URLSessionSocketTransport.swift  # WSS 传输
│   │       └── WSControlFrame.swift           # 帧编解码
│   └── FluentWorkDiagnostics/
│       └── Tracker.swift                     # 事件追踪
└── Tests/
    ├── FluentWorkCoreTests/
    │   ├── Audio/
    │   │   └── LiveAudioEngineTests.swift
    │   └── SpeechSession/
    │       └── SpeechSessionMachineTests.swift
    └── Architecture/
        └── SpeechSessionMiddlewareTests.swift
```
