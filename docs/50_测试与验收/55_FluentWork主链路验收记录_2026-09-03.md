# FluentWork 主链路验收记录（2026-09-03）

**性质**：真机验收记录，配合 `53_FluentWork第一波与第二波App可测试问题验收清单.md` 使用；量化（P90 / 打断 / 锁屏）与弱网降级仍延后。

## 已验收的主链路

| 环节 | 证据 | 状态 |
|---|---|---|
| 真机说话 → Volc Duplex ASR/AI 回复 | 多次真机会话 `collect_turn.done outcome=ok`（session `a607…`、`dc2bdfb3` 等） | ✅ |
| 语料命中 → `feedback.badge`（真实 block） | `phrase_block_id=9e5363ad… score=1 tier=soft` | ✅ |
| 徽章防刷屏（单槽轮换） | iOS `eb79a24`；真机两连击不再叠两条 | ✅ |
| 无命中不显示徽章 | 真机说非语料句未出现徽章 | ✅ |
| 结束本轮 → `session.end` 落库 | iOS `dbf2d94`；gateway `voice.session_lifecycle.end` 200 | ✅ |
| Ark 回顾生成 → ready + 炼化卡 | backend `f725cd5` 场景归一；session `dc2bdfb3` 10.4s `outcome=ok` | ✅ |
| iOS 回顾轮询到 ready | iOS `8c8f064`；日志出现每 2s review GET | ✅ |
| ended 后可重新开始 | iOS（本次记录前修复 + 回归测试） | ✅ |
| 每日一读有内容可读 | 真机页面出现内容；backend `smoke-daily-read` PASS | ✅ |

## 期间修复的链路问题（均已合入）

- backend `77e0578` / `e3c7179` / `a6e206e` / `f725cd5`：
  corpus 内部端点、live provider BadgeEmitter、内存栈进程内 review worker、dev-up 加载 `.env.volc.local` 并剥离行内注释、scene_type 归一化
- iOS `314f08c` / `dbf2d94` / `4aaef7e` / `8c8f064` / `c46d51e`：
  `turn_id` 透传、wire decode 测试、结束前发 `session.end`、场景 `standup`、回顾轮询、全屏可返回

## 仍延后 / OPEN

- I12 真机量化：P90、打断 ≤200ms、锁屏/后台
- 弱网降级与 Outbox replay 实测
- backend #43：Volc 空闲 keepalive / auto-reopen
- backend #28（B15）：真实人工标注样本
- meta #12：火山商务前置
